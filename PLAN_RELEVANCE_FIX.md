# 🎯 ПЛАН: Радикальное улучшение релевантности поиска тендеров

## Статус: ОЖИДАЕТ ОДОБРЕНИЯ

---

## 📊 ДИАГНОСТИКА ПРОБЛЕМ

### Проблема 1: Региональная фильтрация не работает
- Пользователь выбирает ЦФО → получает тендеры из ЯНАО, Тюмени, СПб
- **Причина:** Функция `_expand_federal_districts()` ожидает "Центральный ФО", но получает уже раскрытые регионы ["Москва", "Московская область"...]
- Логика сравнения полностью некорректна

### Проблема 2: Семантическая нерелевантность
- "разработка ПО" матчит "Разработка охранной зоны" (40% релевантность!)
- "логистика" матчит тендеры с "УПРАВЛЕНИЕ ЛОГИСТИКИ" в названии заказчика
- **Причина:** Keyword-based matching без понимания контекста

---

## 🛠 ПЛАН РЕШЕНИЯ

### ЭТАП 1: Срочное исправление региональной фильтрации (30 мин)

**Файл:** `tender_sniper/matching/smart_matcher.py`

**Что сделать:**
1. Убрать функцию `_expand_federal_districts()` - она не нужна, регионы уже раскрыты в wizard
2. Исправить логику сравнения регионов:
   - Привести оба значения к нижнему регистру
   - Использовать fuzzy matching (частичное совпадение)
   - Поддержать варианты написания ("Санкт-Петербург" = "санкт петербург" = "спб")

**Новая логика:**
```python
def _check_region_match(self, filter_regions: List[str], tender_region: str) -> bool:
    """Проверка соответствия региона тендера фильтру."""
    if not filter_regions:
        return True  # Нет ограничений по региону

    if not tender_region:
        return False  # Регион тендера неизвестен - отклоняем

    tender_region_lower = tender_region.lower()

    for region in filter_regions:
        region_lower = region.lower()
        # Проверяем разные варианты совпадения
        if (region_lower in tender_region_lower or
            tender_region_lower in region_lower or
            self._fuzzy_region_match(region_lower, tender_region_lower)):
            return True

    return False
```

---

### ЭТАП 2: AI-фильтрация релевантности (1 час)

**Концепция:** После keyword-matching добавить AI-проверку семантического соответствия.

**Новый модуль:** `tender_sniper/ai_relevance_filter.py`

```python
async def check_semantic_relevance(
    tender_name: str,
    filter_keywords: List[str],
    filter_name: str
) -> tuple[bool, int]:
    """
    Проверяет семантическую релевантность тендера фильтру через AI.

    Returns:
        (is_relevant, confidence_score)
    """
    prompt = f"""Ты эксперт по госзакупкам. Определи, релевантен ли тендер фильтру.

Название тендера: "{tender_name}"
Фильтр пользователя: "{filter_name}"
Ключевые слова фильтра: {filter_keywords}

Вопрос: Этот тендер ДЕЙСТВИТЕЛЬНО соответствует тому, что ищет пользователь?

ВАЖНЫЕ ПРИМЕРЫ:
- "Разработка охранной зоны" НЕ релевантен фильтру "разработка ПО" (это строительство, не IT)
- "Разработка программного обеспечения для учёта" РЕЛЕВАНТЕН фильтру "разработка ПО"
- "Транспортировка медицинских отходов" НЕ релевантен фильтру "логистические услуги" (это утилизация)

Ответь JSON:
{{"relevant": true/false, "confidence": 0-100, "reason": "краткое объяснение"}}
"""

    # Вызов OpenAI GPT-4o-mini (быстрый и дешёвый)
    response = await openai_client.chat.completions.create(...)

    return parse_response(response)
```

**Интеграция в SmartMatcher:**
```python
async def match_tender_with_ai(self, tender, filter_config):
    # 1. Сначала быстрый keyword matching
    basic_match = self.match_tender(tender, filter_config)

    if not basic_match:
        return None

    # 2. Если score < 70, проверяем через AI
    if basic_match['score'] < 70:
        is_relevant, ai_confidence = await check_semantic_relevance(
            tender['name'],
            filter_config['keywords'],
            filter_config['name']
        )

        if not is_relevant:
            logger.info(f"🤖 AI отклонил тендер: {tender['name'][:50]}")
            return None

        # Корректируем score на основе AI
        basic_match['score'] = (basic_match['score'] + ai_confidence) // 2
        basic_match['ai_verified'] = True

    return basic_match
```

---

### ЭТАП 3: Улучшение NEGATIVE_PATTERNS (20 мин)

**Добавить паттерны для исключения нерелевантных тендеров:**

```python
# Для фильтров типа "разработка ПО"
SOFTWARE_NEGATIVE_PATTERNS = [
    'охранн',           # охранная зона, охранные мероприятия
    'проект границ',    # разработка границ территории
    'месторожден',      # разработка месторождений
    'карьер',           # разработка карьера
    'документац',       # разработка проектной документации (если не IT)
    'генплан',          # разработка генерального плана
]

# Для фильтров типа "логистика"
LOGISTICS_NEGATIVE_PATTERNS = [
    'медицинских отходов',
    'биологических образцов',
    'собак',
    'мероприятий',
    'охранных',
]
```

---

### ЭТАП 4: Повышение минимального score (5 мин)

**Текущая настройка:**
```python
min_score = 60  # Слишком низко!
```

**Новая настройка:**
```python
min_score = 75  # Только высокорелевантные тендеры
```

Тендер со score 40 (как "Разработка охранной зоны") не должен показываться!

---

### ЭТАП 5: Логирование для отладки (10 мин)

Добавить INFO-level логи для:
- Входящих регионов фильтра
- Региона тендера
- Результата сравнения регионов
- AI-проверки (если включена)

---

## 📈 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

| До | После |
|----|-------|
| Регионы не фильтруются | Строгая фильтрация по ФО/регионам |
| "разработка" матчит всё | AI отсекает нерелевантное |
| Score 40 проходит | Минимум 75 для показа |
| Пользователи жалуются | Пользователи довольны |

---

## ⏱ ПРИОРИТЕТ ВЫПОЛНЕНИЯ

1. ✅ **ВЫПОЛНЕНО** - Исправлена региональная фильтрация
   - Добавлен метод `_check_region_match()` с fuzzy matching
   - Добавлены REGION_ALIASES (ЯНАО, СПб, ХМАО и др.)
   - Улучшено логирование для отладки
2. ✅ **ВЫПОЛНЕНО** - Поднят минимальный score до 75
3. **ВАЖНО** - Добавить AI-проверку семантики (следующий этап)
4. **ПОЛЕЗНО** - Расширить NEGATIVE_PATTERNS

---

## ✅ ЧЕКЛИСТ ВЫПОЛНЕННЫХ ИЗМЕНЕНИЙ

- [x] Исправлена региональная фильтрация с поддержкой алиасов
- [x] Минимальный score повышен до 75
- [ ] AI-проверка семантики (требует отдельной реализации)

---

**Базовые исправления выполнены. AI-семантика - следующий этап.**
