# Руководство по LLM провайдерам

Система поддерживает 5 различных LLM провайдеров. Выберите подходящий для ваших нужд.

---

## 🚀 1. Groq (РЕКОМЕНДУЕТСЯ - БЕСПЛАТНО!)

### Почему Groq?
- ✅ **БЕСПЛАТНЫЙ** - щедрый free tier
- ✅ **Очень быстрый** - inference на специальном оборудовании
- ✅ **Качественные модели** - Llama 3.1 70B, Mixtral
- ✅ **Простая регистрация**

### Установка:

```bash
# Установить зависимость
pip install groq

# Получить API ключ: https://console.groq.com/
# Зарегистрироваться (бесплатно)
# Создать API ключ
```

### Настройка:

1. **Отредактируйте `config/settings.yaml`:**
```yaml
llm:
  provider: "groq"
  model: null  # Автоматически использует llama-3.1-70b-versatile
```

2. **Создайте `.env`:**
```bash
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxx
```

3. **Запустите:**
```bash
python main.py документ.pdf
```

### Доступные модели Groq:
- `llama-3.1-70b-versatile` (рекомендуется) - 128K контекст
- `llama-3.1-8b-instant` - быстрая, легкая
- `mixtral-8x7b-32768` - хорошее качество

---

## 💰 2. OpenAI GPT (ДЕШЕВЫЙ)

### Почему OpenAI?
- ✅ Высокое качество анализа
- ✅ GPT-4o-mini очень дешевая (~$0.15/1M токенов)
- ✅ $5 бесплатно для новых пользователей
- ✅ Надежная инфраструктура

### Установка:

```bash
pip install openai
```

### Настройка:

1. **Получить API ключ:**
   - Зарегистрироваться на https://platform.openai.com/
   - Перейти в API Keys
   - Создать новый ключ

2. **`config/settings.yaml`:**
```yaml
llm:
  provider: "openai"
  model: "gpt-4o-mini"  # Дешевая и качественная
  # model: "gpt-4o"      # Дороже, но лучше качество
```

3. **`.env`:**
```bash
OPENAI_API_KEY=sk-proj-xxxxxxxxxx
```

### Модели OpenAI:
- `gpt-4o-mini` (рекомендуется) - $0.15/1M токенов
- `gpt-4o` - $5/1M токенов, лучшее качество
- `gpt-3.5-turbo` - $0.50/1M токенов

---

## 🆓 3. Google Gemini (БЕСПЛАТНЫЙ TIER)

### Почему Gemini?
- ✅ Бесплатный tier (60 запросов/минуту)
- ✅ Большой контекст (1M токенов)
- ✅ Хорошее качество анализа
- ✅ От Google

### Установка:

```bash
pip install google-generativeai
```

### Настройка:

1. **Получить API ключ:**
   - https://makersuite.google.com/app/apikey
   - Войти с Google аккаунтом
   - Создать API ключ

2. **`config/settings.yaml`:**
```yaml
llm:
  provider: "gemini"
  model: "gemini-1.5-flash"  # Бесплатный, быстрый
  # model: "gemini-1.5-pro"  # Лучше качество
```

3. **`.env`:**
```bash
GOOGLE_API_KEY=AIzaxxxxxxxxxxxxxxxxxx
```

### Модели Gemini:
- `gemini-1.5-flash` (рекомендуется для free tier)
- `gemini-1.5-pro` - лучше качество, но ограничения free tier

---

## 🏠 4. Ollama (ЛОКАЛЬНО, БЕСПЛАТНО)

### Почему Ollama?
- ✅ **Полностью бесплатно** - работает на вашем ПК
- ✅ **Без API ключей** - приватность данных
- ✅ **Офлайн работа**
- ⚠️ Требует хороший ПК (16+ GB RAM)

### Установка:

1. **Установить Ollama:**
```bash
# macOS
brew install ollama

# Linux
curl https://ollama.ai/install.sh | sh

# Windows: скачать с https://ollama.ai/
```

2. **Скачать модель:**
```bash
# Llama 3.1 8B (требует ~8GB RAM)
ollama pull llama3.1:8b

# Llama 3.1 70B (требует ~48GB RAM)
ollama pull llama3.1:70b

# Qwen 2.5 14B (хорош для русского)
ollama pull qwen2.5:14b
```

3. **Запустить сервер:**
```bash
ollama serve
```

### Настройка:

1. **`config/settings.yaml`:**
```yaml
llm:
  provider: "ollama"
  model: "llama3.1:8b"
  # model: "qwen2.5:14b"  # Лучше для русского языка
  ollama_base_url: "http://localhost:11434"
```

2. **Не нужен API ключ!** (удалите из `.env` или оставьте закомментированным)

3. **Запустите:**
```bash
python main.py документ.pdf
```

### Рекомендуемые модели Ollama:
- `llama3.1:8b` - хороший баланс (8GB RAM)
- `qwen2.5:14b` - отлично для русского (16GB RAM)
- `llama3.1:70b` - лучшее качество (48GB RAM)
- `mixtral:8x7b` - хорошее качество (32GB RAM)

---

## 🎯 5. Anthropic Claude (ОРИГИНАЛЬНЫЙ)

### Почему Claude?
- ✅ Лучшее качество анализа
- ✅ Большой контекст (200K токенов)
- ✅ Отлично понимает инструкции
- ⚠️ Платный, нет free tier

### Установка:

```bash
pip install anthropic
```

### Настройка:

1. **Получить API ключ:**
   - https://console.anthropic.com/
   - Зарегистрироваться
   - Добавить кредиты ($10 минимум)
   - Создать API ключ

2. **`config/settings.yaml`:**
```yaml
llm:
  provider: "anthropic"
  model: "claude-sonnet-4-20250514"
```

3. **`.env`:**
```bash
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxx
```

---

## 📊 Сравнение провайдеров

| Провайдер | Стоимость | Качество | Скорость | Сложность настройки |
|-----------|-----------|----------|----------|---------------------|
| **Groq** | Бесплатно | ⭐⭐⭐⭐ | ⚡⚡⚡⚡⚡ | Очень простая |
| **OpenAI** | $0.15-5/1M | ⭐⭐⭐⭐⭐ | ⚡⚡⚡⚡ | Простая |
| **Gemini** | Бесплатно/Платно | ⭐⭐⭐⭐ | ⚡⚡⚡⚡ | Простая |
| **Ollama** | Бесплатно | ⭐⭐⭐ | ⚡⚡⚡ | Средняя (требует ПК) |
| **Claude** | ~$3/1M | ⭐⭐⭐⭐⭐ | ⚡⚡⚡ | Простая (но платно) |

---

## 🎯 Рекомендации по выбору

### Для начала работы:
**→ Используйте Groq** (бесплатно, быстро, качественно)

### Если нужно лучшее качество:
**→ OpenAI GPT-4o-mini** (дешево) или **Claude Sonnet** (дорого)

### Если важна приватность:
**→ Ollama** (локально на вашем ПК)

### Если есть хороший ПК:
**→ Ollama с qwen2.5:14b** (отлично для русского)

---

## 🔧 Переключение между провайдерами

Достаточно изменить два параметра:

1. **В `config/settings.yaml`:**
```yaml
llm:
  provider: "groq"  # Поменяйте на нужный
```

2. **В `.env`:**
```bash
# Раскомментируйте нужный ключ
GROQ_API_KEY=...
# OPENAI_API_KEY=...
# GOOGLE_API_KEY=...
```

3. **Перезапустите:**
```bash
python main.py документ.pdf
```

---

## ❓ FAQ

### Какой провайдер лучше для русского языка?
- **Groq** с Llama 3.1 70B - хорошо
- **Ollama** с qwen2.5:14b - отлично
- **Claude** Sonnet 4 - отлично (но платно)

### Можно ли использовать без интернета?
Да, только **Ollama** (локально).

### Какой провайдер самый быстрый?
**Groq** - inference на специальном оборудовании (LPU).

### Какой провайдер самый дешевый?
**Groq** и **Gemini** - бесплатные tier'ы.
Из платных - **OpenAI GPT-4o-mini** (~$0.15/1M токенов).

### Как узнать, что провайдер работает?
При запуске main.py выводится информация:
```
LLM провайдер: GROQ
Модель: llama-3.1-70b-versatile
```

### Что делать, если получаю ошибку API?
1. Проверьте правильность API ключа в `.env`
2. Проверьте `provider` в `config/settings.yaml`
3. Убедитесь, что установлена нужная библиотека (`pip install groq`)
4. Проверьте лимиты на аккаунте провайдера

---

## 🚀 Быстрый старт с Groq (БЕСПЛАТНО)

```bash
# 1. Зарегистрироваться
# Откройте: https://console.groq.com/
# Создайте аккаунт (бесплатно)
# Получите API ключ

# 2. Установить зависимости
pip install groq

# 3. Настроить .env
echo "GROQ_API_KEY=gsk_ваш_ключ_здесь" > .env

# 4. Настроить config/settings.yaml
# (по умолчанию уже настроен на Groq)

# 5. Запустить
python main.py документ.pdf

# Готово! 🎉
```

---

**Рекомендация:** Начните с **Groq** - бесплатно, быстро и качественно!
