# 🚀 Инструкция по деплою V2.0 на сервер

## 📦 Что было запушено в GitHub

**Commit:** `b305511` - V2.0: Major Modernization
**Tag:** `v2.0.0-beta`
**Дата:** 24.11.2024

**Изменено файлов:** 8
**Добавлено строк:** +1157
**Удалено строк:** -24

---

## 🔄 Инструкция по обновлению на сервере

### Шаг 1: Подключиться к серверу

```bash
ssh user@your-server.com
cd /path/to/tender-ai-bot
```

### Шаг 2: Остановить текущий бот

```bash
# Найти процесс бота
ps aux | grep "python.*bot.main" | grep -v grep

# Остановить бота (замените PID на реальный)
kill <PID>

# Или если используется Docker:
docker-compose down
```

### Шаг 3: Сделать бэкап базы данных (важно!)

```bash
# Создать бэкап текущей БД
cp bot/data/tender_bot.db bot/data/tender_bot.db.backup_$(date +%Y%m%d_%H%M%S)
```

### Шаг 4: Обновить код

```bash
# Получить последние изменения
git fetch origin

# Посмотреть что изменилось
git log HEAD..origin/main --oneline

# Обновить до V2.0
git pull origin main

# Проверить что на правильном коммите
git log --oneline -1
# Должно быть: b305511 🚀 V2.0: Major Modernization
```

### Шаг 5: Проверить зависимости

```bash
# V2.0 использует те же зависимости, но проверим:
pip3 install -r requirements.txt

# Или если используется виртуальное окружение:
source venv/bin/activate
pip install -r requirements.txt
```

### Шаг 6: Проверить миграцию БД

```bash
# V2.0 автоматически создаст новую таблицу tender_analyses при первом запуске
# Ничего дополнительного делать не нужно

# Можно проверить структуру БД:
sqlite3 bot/data/tender_bot.db ".schema tender_analyses"
```

### Шаг 7: Запустить бота

```bash
# Если запуск через systemd:
sudo systemctl restart tender-bot

# Если запуск вручную:
nohup python3 -m bot.main > bot.log 2>&1 &

# Если используется Docker:
docker-compose up -d
```

### Шаг 8: Проверить работоспособность

```bash
# Проверить логи (первые 50 строк)
tail -50 bot.log

# Должны увидеть:
# ✅ База данных инициализирована
# ✅ Бот успешно запущен!
# Start polling

# Проверить что бот работает
ps aux | grep "python.*bot.main" | grep -v grep

# Проверить что нет конфликтов
tail -f bot.log | grep -i conflict
# Должно быть пусто
```

---

## 🧪 Тестирование V2.0 функций

### Тест 1: Проверка кэширования

1. Открыть бота в Telegram
2. Выполнить поиск тендера
3. Проанализировать тендер → **первый раз (медленно)**
4. Анализировать тот же тендер снова → **должно быть мгновенно из кэша**
5. В логах должно появиться: `✅ CACHE HIT`

### Тест 2: Проверка новой таблицы

```bash
# Подключиться к БД
sqlite3 bot/data/tender_bot.db

# Проверить что таблица создана
.tables
# Должна быть: tender_analyses

# Посмотреть структуру
.schema tender_analyses

# Проверить записи (после анализа тендера)
SELECT tender_number, score, recommendation, created_at FROM tender_analyses LIMIT 5;

.quit
```

### Тест 3: Статистика кэша

```python
# В Python shell на сервере
python3 << EOF
import asyncio
from bot.db import get_database

async def check_cache():
    db = await get_database()
    stats = await db.get_cache_stats()
    print("Статистика кэша:")
    print(f"  Всего записей: {stats['total_cached']}")
    print(f"  Валидных: {stats['valid_cached']}")
    print(f"  Истекших: {stats['expired_cached']}")
    print(f"  Средний score: {stats['average_score']}")
    print(f"  Рекомендации: {stats['recommendations']}")

asyncio.run(check_cache())
EOF
```

---

## ⚠️ Возможные проблемы и решения

### Проблема 1: Конфликт ботов

**Ошибка:** `TelegramConflictError: terminated by other getUpdates request`

**Решение:**
```bash
# Убедиться что старый процесс остановлен
pkill -f "python.*bot.main"

# Подождать 10 секунд
sleep 10

# Запустить снова
python3 -m bot.main
```

### Проблема 2: Ошибка импорта

**Ошибка:** `ModuleNotFoundError: No module named 'batch'`

**Решение:**
```bash
# Проверить что новые файлы есть
ls -la src/batch/

# Должны быть:
# __init__.py
# batch_processor.py

# Если нет - обновить код снова
git pull origin main
```

### Проблема 3: База данных заблокирована

**Ошибка:** `sqlite3.OperationalError: database is locked`

**Решение:**
```bash
# Остановить все процессы бота
pkill -f "python.*bot.main"

# Проверить что нет процессов держащих БД
lsof | grep tender_bot.db

# Если есть - убить их
kill <PID>

# Запустить снова
python3 -m bot.main
```

---

## 📊 Мониторинг после деплоя

### Что проверять первые 24 часа:

1. **Логи на ошибки:**
   ```bash
   tail -f bot.log | grep -E "ERROR|WARNING"
   ```

2. **Использование памяти:**
   ```bash
   ps aux | grep "python.*bot.main"
   # Колонка RSS - память в KB
   ```

3. **Рост БД:**
   ```bash
   watch -n 60 'ls -lh bot/data/tender_bot.db'
   ```

4. **Cache hit rate:**
   ```bash
   grep "CACHE HIT\|CACHE MISS" bot.log | tail -20
   ```

---

## 🎯 Критерии успешного деплоя

- ✅ Бот запущен без ошибок
- ✅ Таблица `tender_analyses` создана
- ✅ Первый анализ тендера работает
- ✅ Повторный анализ использует кэш (мгновенно)
- ✅ Логи показывают `CACHE HIT` при повторных запросах
- ✅ Нет конфликтов с другими экземплярами
- ✅ База данных растет (записи кэша добавляются)

---

## 📞 В случае проблем

1. Проверить логи: `tail -100 bot.log`
2. Проверить коммит: `git log --oneline -1`
3. Откатиться на предыдущую версию:
   ```bash
   git checkout a676c93  # Последняя стабильная версия до V2.0
   sudo systemctl restart tender-bot
   ```
4. Восстановить БД из бэкапа:
   ```bash
   cp bot/data/tender_bot.db.backup_YYYYMMDD_HHMMSS bot/data/tender_bot.db
   ```

---

## 🚀 После успешного деплоя

1. Протестировать на 9 примерах "хороших" тендеров
2. Проверить что НМЦК корректно отображается
3. Проверить работу кэша (повторный анализ)
4. Мониторить первые 24 часа

**Следующий шаг:** V2.1 с Excel Export и batch processing в боте
