# 🤖 TenderBot - Telegram бот для анализа тендеров

Telegram бот для умного поиска и AI-анализа тендеров на zakupki.gov.ru.

## 🚀 Быстрый старт

### 1. Создайте Telegram бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен - он понадобится для настройки

### 2. Настройте конфигурацию

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp ../.env.example ../.env
   ```

2. Откройте `.env` и укажите:
   ```env
   OPENAI_API_KEY=ваш_ключ_openai
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   ```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

Основные зависимости:
- `aiogram==3.15.0` - асинхронный фреймворк для Telegram ботов
- `aiosqlite` - асинхронная база данных для истории поисков
- `python-dotenv` - загрузка переменных окружения

### 4. Запустите бота

```bash
cd bot
python3 main.py
```

Вы увидите:
```
✅ Конфигурация валидна
🤖 Бот запускается...
✅ Бот успешно запущен!
```

### 5. Начните использовать

1. Найдите вашего бота в Telegram по имени
2. Отправьте `/start`
3. Следуйте инструкциям бота

## 📋 Возможности

### ✅ Реализовано

- **Команда /start** - приветствие и главное меню
- **Команда /help** - помощь по использованию
- **Главное меню** с кнопками:
  - 🔍 Новый поиск
  - 📂 Мои поиски
  - ℹ️ Помощь
- **Система состояний (FSM)** для управления диалогом
- **Клавиатуры** для удобного взаимодействия

### 🔄 В разработке

- Пошаговый ввод параметров поиска
- Интеграция с системой поиска тендеров
- Отображение результатов
- AI-анализ документов
- База данных для истории поисков

## 🏗️ Архитектура

```
bot/
├── main.py              # Точка входа, запуск бота
├── config.py            # Конфигурация бота
├── handlers/            # Обработчики команд и сообщений
│   ├── start.py         # /start, /help, главное меню
│   └── ...
├── keyboards/           # Клавиатуры для взаимодействия
│   └── __init__.py      # Основные клавиатуры
├── states/              # Состояния FSM
│   └── __init__.py      # SearchStates, HistoryStates
├── database/            # База данных
│   └── bot.db           # SQLite база (создается автоматически)
└── utils/               # Вспомогательные функции
```

## 🎯 User Flow

1. **Начало работы**
   - `/start` → Приветствие → Главное меню

2. **Новый поиск**
   - "🔍 Новый поиск"
   - Шаг 1: Ввод поискового запроса
   - Шаг 2: Выбор ценового диапазона
   - Шаг 3: Выбор количества тендеров
   - → Поиск → Результаты

3. **Просмотр результатов**
   - Краткий список тендеров
   - Кнопка "📄 Подробнее" → Детали тендера
   - Кнопка "🤖 Анализировать" → AI-анализ

4. **История поисков**
   - "📂 Мои поиски"
   - Список предыдущих поисков
   - Возможность повторить поиск

## 🛠️ Разработка

### Добавление нового обработчика

1. Создайте файл в `handlers/`:
   ```python
   from aiogram import Router
   router = Router()

   @router.message(...)
   async def your_handler(message: Message):
       ...
   ```

2. Зарегистрируйте в `main.py`:
   ```python
   from bot.handlers import your_module
   dp.include_router(your_module.router)
   ```

### Добавление нового состояния

В `states/__init__.py`:
```python
class YourStates(StatesGroup):
    your_state = State()
```

### Добавление клавиатуры

В `keyboards/__init__.py`:
```python
def get_your_keyboard() -> InlineKeyboardMarkup:
    builder = InlineKeyboardBuilder()
    builder.row(...)
    return builder.as_markup()
```

## 📝 Логирование

Логи сохраняются в `bot/bot.log` и выводятся в консоль.

Уровни логирования:
- `INFO` - основные события (запуск, команды)
- `ERROR` - ошибки выполнения
- `WARNING` - предупреждения

## 🔒 Безопасность

- Никогда не коммитьте `.env` файл с реальными токенами
- Используйте `.env.example` как шаблон
- Токен бота и API ключи храните в переменных окружения

## 🐛 Отладка

### Бот не запускается

1. Проверьте, что `TELEGRAM_BOT_TOKEN` указан в `.env`
2. Проверьте, что токен валидный (получен от @BotFather)
3. Проверьте логи в `bot/bot.log`

### Бот не отвечает

1. Убедитесь, что бот запущен (`python3 main.py`)
2. Проверьте интернет-соединение
3. Проверьте логи на наличие ошибок

## 📖 Дополнительные ресурсы

- [Документация aiogram](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [BotFather](https://t.me/BotFather) - создание и управление ботами

## 🤝 Следующие шаги

1. Реализовать пошаговый ввод параметров поиска
2. Интегрировать с существующей системой поиска
3. Добавить отображение результатов
4. Реализовать AI-анализ
5. Добавить базу данных для истории
