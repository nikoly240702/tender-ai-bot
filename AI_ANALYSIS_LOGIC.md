# Логика AI-анализа тендеров

## Обзор системы

Система анализа тендеров состоит из следующих основных компонентов:

1. **Извлечение текста из документов** (PDF/DOCX)
2. **Приоритетный анализ проекта контракта** (если найден)
3. **Основной анализ документации через LLM**
4. **Детекция пробелов в информации**
5. **Генерация вопросов для заказчика**
6. **Извлечение контактов**
7. **Генерация отчетов** (HTML, JSON, Markdown)

---

## Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                    TenderAnalysisAgent                       │
│                        (main.py)                             │
│                                                               │
│  Координирует все этапы анализа:                            │
│  1. Извлечение текста                                        │
│  2. Анализ контракта                                         │
│  3. Анализ документации                                      │
│  4. Детекция пробелов                                        │
│  5. Генерация вопросов                                       │
│  6. Извлечение контактов                                     │
│  7. Генерация отчетов                                        │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────┴──────────────────┐
        │                                      │
        ▼                                      ▼
┌───────────────────┐              ┌───────────────────┐
│  TenderAnalyzer   │              │   LLMAdapter      │
│ (tender_analyzer) │◄─────────────┤  (llm_adapter)    │
│                   │              │                   │
│ - analyze_docs()  │              │ - query()         │
│ - detect_gaps()   │              │ - Groq API        │
│ - extract_info()  │              │ - Anthropic API   │
└───────────────────┘              └───────────────────┘

---

## 🔑 Ключевые файлы системы

### 1. main.py - Главный оркестратор

**Путь**: `/Users/nikolaichizhik/tender-ai-agent/main.py`

**Назначение**: Координирует весь процесс анализа тендера

**Основной метод**:
```python
def analyze_tender(self, file_paths: List[str]) -> Dict[str, Any]:
    """
    Полный анализ тендера
    
    Этапы:
    1. Извлечение текста из документов
    2. Приоритетный анализ проекта контракта
    3. Анализ документации через LLM
    4. Детекция пробелов
    5. Генерация вопросов
    6. Извлечение контактов
    7. Генерация отчетов
    """
```

---

### 2. tender_analyzer.py - Основной анализатор

**Путь**: `/Users/nikolaichizhik/tender-ai-agent/src/analyzers/tender_analyzer.py`

**Назначение**: Содержит ВСЕ промпты для LLM анализа

**Ключевые методы**:

#### analyze_documentation()
- **Лимит**: 60,000 символов документации
- **Модель**: gpt-4o (критичная задача)
- **Структура промпта**: Event-Context-Subject-Object-Goal-Task-Means-Actions-Result

**Промпт включает**:
1. Системный промпт с ролью эксперта
2. Методичный поиск по разделам документации
3. Извлечение ключевой информации:
   - ИНН заказчика
   - Сроки (подача заявок, исполнение, этапы)
   - Условия оплаты
   - Технические требования
   - Обеспечения

**JSON ответ** содержит:
- `tender_name`: Название закупки
- `customer`: Полное название заказчика
- `customer_inn`: ИНН заказчика
- `nmck`: НМЦК
- `deadline_submission`: Срок подачи заявок
- `deadline_execution`: Срок исполнения
- `execution_stages`: Этапы выполнения
- `payment_terms`: Условия оплаты
- `technical_requirements`: Технические требования
- `application_guarantee`: Обеспечение заявки
- `contract_guarantee`: Обеспечение контракта

#### detect_gaps()
- **Лимит**: 60,000 символов
- **Модель**: gpt-4o (критичная задача)
- **Особенность**: Двухэтапная проверка пробелов

**Процесс**:
1. **Этап 1**: Поиск информации по ВСЕЙ документации
2. **Этап 2**: Фиксация ТОЛЬКО реальных пробелов

**Критичные правила**:
- НЕ включать информацию, которая УЖЕ ЕСТЬ в документах
- Проверять ВСЕ разделы перед добавлением пробела
- Качество важнее количества
- Если пробелов нет → вернуть `[]`

---

### 3. llm_adapter.py - Адаптер для LLM

**Путь**: `/Users/nikolaichizhik/tender-ai-agent/src/analyzers/llm_adapter.py`

**Назначение**: Унифицированный интерфейс к различным LLM провайдерам

**Поддерживаемые провайдеры**:
- Groq (llama3-70b-8192)
- OpenAI (gpt-4o, gpt-4o-mini)
- Anthropic (claude-3-5-sonnet-20241022)

**Гибридный режим**:
- **Критичные задачи** → gpt-4o
- **Простые задачи** → gpt-4o-mini

---

## 📋 Подробное описание промптов

### ПРОМПТ 1: analyze_documentation()

```
# СОБЫТИЕ
Компания рассматривает участие в государственной закупке и нуждается в детальном анализе тендерной документации для принятия обоснованного решения.

# КОНТЕКСТ
Компания специализируется на [profile]. Необходимо провести комплексный анализ тендерной документации, чтобы понять:
- Соответствие требованиям тендера
- Финансовую целесообразность участия
- Риски и возможности
- Полноту предоставленной информации

# СУБЪЕКТ
Ты — эксперт-аналитик по государственным закупкам в России с 15-летним опытом.

# ОБЪЕКТ
Тендерная документация и профиль компании-участника.

ТЕНДЕРНАЯ ДОКУМЕНТАЦИЯ:
{documentation_text[:60000]}

ПРОФИЛЬ КОМПАНИИ:
{company_profile}

# ЦЕЛЬ
Провести комплексный анализ тендерной документации для принятия решения об участии.

# ЗАДАЧА
Извлечь все критически важные параметры: информацию о заказчике, сроки, требования, финансовые условия, риски.

# СРЕДСТВА
- Тендерная документация (контракты, ТЗ, извещения)
- Профиль компании с компетенциями
- Знание законодательства (44-ФЗ, 223-ФЗ)
- Опыт анализа тысяч тендеров

# ДЕЙСТВИЯ

## Этап 1: МЕТОДИЧНЫЙ ПОИСК ИНФОРМАЦИИ

1. **АНАЛИЗ СТРУКТУРЫ ДОКУМЕНТАЦИИ**
   - Извещение о закупке
   - Техническое задание (ТЗ) / Спецификация
   - Проект контракта / Проект договора
   - Инструкция по заполнению заявки
   - Приложения (графики, формы, образцы)

2. **ИЗВЛЕЧЕНИЕ БАЗОВОЙ ИНФОРМАЦИИ**
   - Название закупки
   - Полное наименование заказчика с ИНН
   - НМЦК (начальная максимальная цена контракта)
   - Размер обеспечения заявки и обеспечения контракта

3. **КРИТИЧНО: ИЗВЛЕЧЕНИЕ СРОКОВ**

   a) **Срок подачи заявок**:
      - Извещение о закупке - раздел "Порядок подачи заявок"
      - Формат: дата и время (например: "до 15.03.2024 10:00 МСК")

   b) **Срок исполнения контракта / Срок поставки**:
      - ПРОЕКТ КОНТРАКТА - раздел "Срок действия контракта"
      - ТЕХНИЧЕСКОЕ ЗАДАНИЕ - раздел "Сроки выполнения работ"
      - ПРИЛОЖЕНИЯ - "График выполнения работ", "Календарный план"
      
      Формат:
      - Абсолютный: "до 31.12.2024"
      - Относительный: "в течение 30 календарных дней с даты заключения контракта"

   c) **Этапы выполнения работ** (если есть):
      - График поставки
      - Календарный план
      - Приложение с разбивкой по этапам

   d) **Срок оплаты**:
      - ПРОЕКТ КОНТРАКТА - раздел "Порядок расчетов"
      - Искать фразы: "в течение X дней", "после подписания акта", "после приемки"

4. **ИЗВЛЕЧЕНИЕ ТЕХНИЧЕСКИХ ТРЕБОВАНИЙ**
   - Спецификация оборудования/товаров/услуг
   - Требования к квалификации исполнителя
   - Требования к наличию лицензий, сертификатов
   - Специфические требования законодательства

5. **ФИНАНСОВЫЕ УСЛОВИЯ**
   - НМЦК и порядок формирования цены
   - Обеспечение заявки (размер и форма)
   - Обеспечение контракта (размер и форма)
   - Условия оплаты и график платежей
   - Штрафные санкции и ответственность

## Этап 2: СОПОСТАВЛЕНИЕ С ПРОФИЛЕМ КОМПАНИИ

ПРОФИЛЬ КОМПАНИИ содержит:
- Основные виды деятельности
- Лицензии и сертификаты
- Опыт выполнения аналогичных работ
- Финансовые возможности
- Технические возможности

Сопоставь требования тендера с возможностями компании.

## Этап 3: АНАЛИЗ РИСКОВ

Выяви потенциальные риски:
- Технические (невозможность выполнения требований)
- Финансовые (недостаточная маржинальность)
- Юридические (несоответствие требованиям)
- Временные (сжатые сроки)
- Репутационные

# РЕЗУЛЬТАТ

Верни JSON строго по следующей схеме:

```json
{
  "tender_name": "Точное название закупки из документации",
  "customer": "Полное наименование заказчика",
  "customer_inn": "ИНН заказчика если найден",
  "nmck": 1234567.89,
  "deadline_submission": "YYYY-MM-DD HH:MM или описание срока",
  "deadline_execution": "YYYY-MM-DD ИЛИ описание относительного срока",
  "execution_stages": [
    {
      "stage_name": "Название этапа",
      "deadline": "Срок выполнения этапа",
      "description": "Описание что должно быть сделано"
    }
  ],
  "payment_terms": {
    "payment_deadline": "ТОЧНЫЙ срок оплаты",
    "payment_moment": "От какого события отсчитывается срок",
    "advance_payment": "Размер аванса если есть"
  },
  "technical_requirements": {
    "summary": "Краткое описание технических требований",
    "key_requirements": ["Требование 1", "Требование 2"],
    "licenses_required": ["Лицензия 1", "Лицензия 2"]
  },
  "application_guarantee": 12345.67,
  "contract_guarantee": 67890.12,
  "risks": [
    {
      "category": "TECHNICAL|FINANCIAL|LEGAL|TIME|REPUTATION",
      "description": "Описание риска",
      "severity": "CRITICAL|HIGH|MEDIUM|LOW"
    }
  ]
}
```

КРИТИЧНО:
- ВСЕ числовые значения должны быть числами, НЕ строками
- Если информация не найдена → null
- Даты в формате ISO 8601: "YYYY-MM-DD"
```

---

### ПРОМПТ 2: detect_gaps()

```
# СОБЫТИЕ
Компания планирует участвовать в тендере и должна выявить РЕАЛЬНЫЕ пробелы в документации.

# КОНТЕКСТ
Уже выполнен базовый анализ тендера. Необходимо проверить документацию на наличие недостающей информации, которая критична для подготовки заявки.

# СУБЪЕКТ
Ты — эксперт по аудиту тендерной документации с опытом проверки сотен закупок.

# ОБЪЕКТ
Тендерная документация для аудита на полноту.

ДОКУМЕНТАЦИЯ:
{documentation_text[:60000]}

# ЦЕЛЬ
Выявить ВСЕ пробелы, противоречия, неясности и недостающую информацию.

# ЗАДАЧА
Провести детальный аудит документации и составить полный список РЕАЛЬНЫХ проблем.

# КРИТИЧНО ВАЖНО
Ты должен выявлять ТОЛЬКО реальные пробелы - информацию, которой ДЕЙСТВИТЕЛЬНО НЕТ в документации.
НЕ задавай вопросы о том, что уже указано в документах!

# ДЕЙСТВИЯ

## КРИТИЧНО: ДВУХЭТАПНАЯ ПРОВЕРКА ПРОБЕЛОВ

### Этап 1: ПОИСК ИНФОРМАЦИИ ПО ВСЕЙ ДОКУМЕНТАЦИИ

Перед тем как отметить информацию как отсутствующую, ПРОВЕРЬ ВСЕ РАЗДЕЛЫ:

1. **Сроки подачи заявок**: Извещение о закупке, Документация
2. **Сроки исполнения контракта**: Проект контракта, ТЗ, Приложения с графиками
3. **Технические характеристики**: ТЗ, Спецификация, Приложения к ТЗ
4. **Условия оплаты**: Проект контракта - ТОЛЬКО! НЕ извещение, НЕ ТЗ
5. **Обеспечения**: Извещение, Документация о закупке
6. **Контактная информация**: Извещение, Документация, Проект контракта

### Этап 2: ФИКСАЦИЯ РЕАЛЬНЫХ ПРОБЕЛОВ

ТОЛЬКО после того, как ты проверил ВСЕ разделы, фиксируй пробел ЕСЛИ:
- Информация ДЕЙСТВИТЕЛЬНО отсутствует
- Информация противоречива (есть в одном месте, но противоречит другому)
- Информация неполная (частично указана, но нужны детали)

### Категории пробелов

1. **CRITICAL** - Без этой информации невозможно подать заявку:
   - Отсутствие сроков подачи заявок
   - Отсутствие требований к содержанию заявки
   - Отсутствие критериев оценки

2. **HIGH** - Информация важна для принятия решения:
   - Неясные условия оплаты
   - Неопределенные технические требования
   - Отсутствие графика выполнения работ

3. **MEDIUM** - Желательно знать, но не критично:
   - Дополнительные требования к участникам
   - Уточнение по комплектации
   - Детали по гарантийному обслуживанию

4. **LOW** - Справочная информация:
   - Дополнительные контакты
   - История предыдущих закупок
   - Рекомендации по оформлению документов

# РЕЗУЛЬТАТ

Верни JSON массив пробелов:

```json
[
  {
    "category": "Категория (например: Сроки, Технические требования, Финансы)",
    "issue": "Описание проблемы",
    "criticality": "CRITICAL|HIGH|MEDIUM|LOW",
    "where_to_find": "В каком разделе обычно находится эта информация",
    "question": "Конкретный вопрос для уточнения"
  }
]
```

КРИТИЧНО ВАЖНЫЕ ПРАВИЛА:
1. **НЕ ВКЛЮЧАЙ В СПИСОК**, если информация ЕСТЬ где-то в документации
2. **ОБЯЗАТЕЛЬНО ПРОВЕРЬ** перед добавлением пробела:
   - Проект контракта
   - Техническое задание
   - Все приложения
   - Извещение о закупке
3. **КАЧЕСТВО ВАЖНЕЕ КОЛИЧЕСТВА**: Лучше 3 реальных пробела, чем 10 ложных вопросов
4. **ФОРМАТ ОТВЕТА**: Если пробелов НЕТ - верни [] - это НОРМАЛЬНО!

ПРИМЕРЫ ЛОЖНЫХ ПРОБЕЛОВ (НЕ ВКЛЮЧАЙ):
- "Нет информации о сроках оплаты" ← если срок указан в проекте контракта
- "Не указаны технические характеристики" ← если они есть в ТЗ
- "Отсутствует обеспечение заявки" ← если размер указан в извещении

ПРИМЕРЫ РЕАЛЬНЫХ ПРОБЕЛОВ (ВКЛЮЧАЙ):
- "В ТЗ указано 'сертифицированное оборудование', но не уточняется какой именно сертификат"
- "В проекте контракта написано 'оплата по факту', но не указан срок в днях"
- "Противоречие: в извещении срок 30 дней, в ТЗ - 45 дней"
```

---

## 🔄 Процесс анализа (пошагово)

### Шаг 1: Извлечение текста
```python
from pdf_extractor import PDFExtractor

extractor = PDFExtractor()
documents = []
for file_path in file_paths:
    text = extractor.extract_text(file_path)
    documents.append({
        'filename': filename,
        'text': text,
        'type': classify_document_type(filename)
    })
```

### Шаг 2: Приоритетный анализ контракта
```python
# Если найден проект контракта - анализируем его первым
contract_doc = find_document_by_type(documents, 'contract')
if contract_doc:
    contract_analysis = analyze_contract_priority(contract_doc['text'])
```

### Шаг 3: Основной анализ документации
```python
# Объединяем весь текст (до 60K символов)
full_text = combine_documents(documents, max_length=60000)

# Анализируем через LLM
analysis = tender_analyzer.analyze_documentation(
    documentation_text=full_text,
    company_profile=company_profile
)
```

### Шаг 4: Детекция пробелов
```python
gaps = tender_analyzer.detect_gaps(
    documentation_text=full_text
)
```

### Шаг 5: Генерация вопросов
```python
questions = generate_questions_for_customer(
    gaps=gaps,
    tender_info=analysis
)
```

### Шаг 6: Извлечение контактов
```python
contacts = extract_contacts(full_text)
```

### Шаг 7: Генерация отчетов
```python
report_generator.generate_html_report(data, tender_name)
report_generator.generate_json_report(data, tender_name)
report_generator.generate_markdown_report(data, tender_name)
```

---

## ⚙️ Конфигурация LLM

### Гибридный режим (по умолчанию)

```yaml
llm:
  provider: "HYBRID"
  primary_model: "gpt-4o"      # Для критичных задач
  fallback_model: "gpt-4o-mini" # Для простых задач
  
  # Критичные задачи → primary_model:
  critical_tasks:
    - analyze_documentation
    - detect_gaps
    - analyze_contract
  
  # Простые задачи → fallback_model:
  simple_tasks:
    - extract_contacts
    - classify_document
```

### Лимиты и ограничения

```python
MAX_DOCUMENTATION_LENGTH = 60000  # символов
MAX_TOKENS_PER_REQUEST = 30000    # токенов для OpenAI
TIMEOUT = 120                     # секунд
MAX_RETRIES = 3                   # попыток при ошибке
```

---

## 📊 Формат результата анализа

### JSON Schema

```json
{
  "tender_info": {
    "tender_name": "string",
    "customer": "string",
    "customer_inn": "string|null",
    "nmck": "number",
    "deadline_submission": "string",
    "deadline_execution": "string",
    "execution_stages": [
      {
        "stage_name": "string",
        "deadline": "string",
        "description": "string"
      }
    ],
    "payment_terms": {
      "payment_deadline": "string",
      "payment_moment": "string",
      "advance_payment": "string|null"
    },
    "technical_requirements": {
      "summary": "string",
      "key_requirements": ["string"],
      "licenses_required": ["string"]
    },
    "application_guarantee": "number",
    "contract_guarantee": "number"
  },
  "gaps": [
    {
      "category": "string",
      "issue": "string",
      "criticality": "CRITICAL|HIGH|MEDIUM|LOW",
      "where_to_find": "string",
      "question": "string"
    }
  ],
  "questions": {
    "critical": ["string"],
    "important": ["string"],
    "clarification": ["string"]
  },
  "contacts": {
    "customer_contact": {
      "name": "string",
      "phone": "string",
      "email": "string"
    },
    "responsible_person": {
      "name": "string",
      "position": "string",
      "phone": "string",
      "email": "string"
    }
  },
  "risks": [
    {
      "category": "TECHNICAL|FINANCIAL|LEGAL|TIME|REPUTATION",
      "description": "string",
      "severity": "CRITICAL|HIGH|MEDIUM|LOW"
    }
  ]
}
```

---

## 🚀 Оптимизации и улучшения

### Текущие оптимизации

1. **Ограничение длины текста**: 60,000 символов (вместо 100,000)
   - Причина: Избежание rate limit OpenAI (30K TPM)
   - Решение: Приоритизация важных разделов

2. **Гибридный режим LLM**:
   - Критичные задачи → gpt-4o (более точный)
   - Простые задачи → gpt-4o-mini (быстрее и дешевле)

3. **Двухэтапная проверка пробелов**:
   - Этап 1: Поиск по ВСЕЙ документации
   - Этап 2: Фиксация ТОЛЬКО реальных пробелов

### Возможные улучшения

1. **RAG (Retrieval-Augmented Generation)**:
   - Разбить документацию на чанки
   - Использовать векторную БД для поиска
   - Анализировать только релевантные части

2. **Многопроходный анализ**:
   - Проход 1: Извлечение структуры
   - Проход 2: Детальный анализ каждого раздела
   - Проход 3: Кросс-проверка и валидация

3. **Кэширование промптов**:
   - Использовать prompt caching Anthropic
   - Экономия токенов на повторяющихся частях

---

## 📝 Примеры использования

### Пример 1: Анализ простого тендера

```python
from main import TenderAnalysisAgent

agent = TenderAnalysisAgent()
result = agent.analyze_tender([
    '/path/to/notice.pdf',
    '/path/to/contract.pdf',
    '/path/to/technical_spec.pdf'
])

print(f"Название: {result['tender_info']['tender_name']}")
print(f"НМЦК: {result['tender_info']['nmck']:,.2f} руб.")
print(f"Найдено пробелов: {len(result['gaps'])}")
```

### Пример 2: Только детекция пробелов

```python
from src.analyzers.tender_analyzer import TenderAnalyzer

analyzer = TenderAnalyzer()
gaps = analyzer.detect_gaps(documentation_text)

for gap in gaps:
    print(f"[{gap['criticality']}] {gap['category']}: {gap['issue']}")
```

---

## 🔍 Отладка и логирование

### Включение подробного логирования

```python
import logging

logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)
```

### Проверка использования токенов

```python
# В llm_adapter.py логируется использование токенов
logger.info(f"Tokens used: {usage.total_tokens} (prompt: {usage.prompt_tokens}, completion: {usage.completion_tokens})")
```

---

## ⚠️ Известные ограничения

1. **Лимит длины текста**: Максимум 60K символов → большие документы обрезаются
2. **Точность извлечения дат**: Зависит от формата в документах
3. **Распознавание сложных таблиц**: PDF с картинками могут не распознаться
4. **Rate limits**: OpenAI TPM ограничение 30,000 токенов/минуту

---

## 📚 Дополнительные файлы

Полный исходный код доступен в следующих файлах:

1. `/Users/nikolaichizhik/tender-ai-agent/main.py`
2. `/Users/nikolaichizhik/tender-ai-agent/src/analyzers/tender_analyzer.py`
3. `/Users/nikolaichizhik/tender-ai-agent/src/analyzers/llm_adapter.py`
4. `/Users/nikolaichizhik/tender-ai-agent/src/extractors/pdf_extractor.py`
5. `/Users/nikolaichizhik/tender-ai-agent/src/reporting/report_generator.py`

---

**Дата создания**: 2025-11-14  
**Версия**: 1.0  
**Автор**: AI Analysis System
