# 🤖 Tender AI Bot - Работа с Claude через терминал

**Версия проекта:** 01.12.2024
**Последнее обновление:** 1 декабря 2024

---

## 📋 Текущее состояние проекта

### ✅ Что работает:
- **RSS парсинг zakupki.gov.ru** - получение актуальных тендеров
- **AI расширение запросов** - GPT-4o-mini генерирует связанные термины
- **SmartMatcher** - ранжирование тендеров по релевантности
- **Фильтрация по типу** - СТРОГАЯ фильтрация товары/услуги/работы
- **Telegram бот** - полный интерфейс для пользователей
- **HTML отчеты** - красивые отчеты с результатами поиска
- **Прокси поддержка** - SOCKS5 прокси для zakupki.gov.ru

### 🔧 Последние исправления (01.12.2024):
1. **Исправлено обогащение данных** - регион и срок подачи теперь отображаются
2. **Строгая фильтрация типов** - медицинские услуги не попадают в товары
3. **Фильтр старых тендеров** - тендеры старше 2 лет исключаются
4. **Двойная проверка** - фильтрация на уровне RSS + на уровне scoring

### ⚠️ Известные проблемы:
1. **Обогащение не запускается** - логи показывают что `enrich_tender_from_page` не вызывается
2. **Регион/дедлайн = Н/Д** - несмотря на исправления, данные не заполняются
3. **Нужна диагностика** - требуется подробное логирование для выяснения причин

---

## 🚀 Настройка для работы через Claude Desktop (терминал)

### Почему через терминал лучше?

**Веб версия (claude.ai):**
- ❌ Не может пушить в `main` - только в ветки `claude/*-sessionID`
- ❌ Требует ручного merge через GitHub UI
- ⏱️ Медленнее - нужно ждать PR

**Desktop версия (терминал):**
- ✅ Может пушить напрямую в `main`
- ✅ Автоматический деплой на Railway
- ⚡ Быстрее - нет промежуточных шагов

---

## 📦 Установка и настройка

### Шаг 1: Создание новой папки проекта

```bash
# Создаем новую папку на Desktop
mkdir -p ~/Desktop/"Агент по тендерам версия 01.12"
cd ~/Desktop/"Агент по тендерам версия 01.12"

# Клонируем актуальный код
git clone https://github.com/nikoly240702/tender-ai-bot.git .

# Проверяем что на main ветке
git checkout main
git pull origin main
```

### Шаг 2: Проверка окружения

```bash
# Проверяем Python
python3 --version  # Должно быть 3.11+

# Устанавливаем зависимости
pip3 install -r requirements.txt

# Проверяем .env файл
cat .env
```

### Шаг 3: Настройка Claude Desktop

1. **Установите Claude Desktop** (если еще не установлен)
   - Скачайте с официального сайта Anthropic

2. **Откройте проект в Claude Code:**
   ```bash
   cd ~/Desktop/"Агент по тендерам версия 01.12"
   claude .
   ```

3. **В Claude Code терминале выполните:**
   ```
   /help
   ```

---

## 🔑 Важные команды для работы

### Git workflow через Claude Desktop:

```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Закоммитить (Claude сделает это за вас)
# Просто попросите: "Закоммить изменения с сообщением: ..."

# Пушить (Claude сделает это автоматически)
git push origin main
```

### Типичный workflow:

1. **Вы:** "Claude, исправь проблему с обогащением тендеров"
2. **Claude:**
   - Анализирует код
   - Вносит изменения
   - Коммитит: `git commit -m "fix: ..."`
   - Пушит: `git push origin main`
   - Railway автоматически деплоит

---

## 🐛 Чек-лист для отладки текущих проблем

### Проблема: Регион и дедлайн = Н/Д

**Что проверить:**
1. ✅ Код `enrich_tender_from_page` исправлен (использует `self.session` с прокси)
2. ✅ Паттерны извлечения обновлены под реальную структуру zakupki.gov.ru
3. ❓ Вызывается ли функция вообще? (нужно добавить логи)
4. ❓ Успешно ли загружается страница? (проверить через прокси)
5. ❓ Срабатывают ли паттерны? (логировать результаты)

**Команды для Claude:**
```
1. Добавь подробное логирование в enrich_tender_from_page
2. Проверь что функция вызывается для каждого тендера
3. Залогируй результаты извлечения (цена, дедлайн, регион)
4. Протестируй на реальном HTML файле
```

### Проблема: Медицинские услуги в товарах

**Что сделано:**
- ✅ Добавлена строгая фильтрация в RSS парсере
- ✅ Добавлена двойная проверка перед scoring
- ✅ Фильтр старых тендеров (>2 лет)

**Что проверить:**
- ❓ Работает ли фильтрация (смотреть логи `⛔ Отфильтрован`)
- ❓ Попадают ли услуги в `search_results`
- ❓ Исключаются ли они в цикле `matches`

---

## 📊 Структура проекта

```
tender-ai-bot/
├── bot/                        # Telegram бот
│   ├── handlers/              # Обработчики команд
│   └── db.py                  # База данных
├── src/
│   └── parsers/
│       └── zakupki_rss_parser.py  # 🔥 RSS парсер (основной код)
├── tender_sniper/
│   ├── instant_search.py      # 🔥 Поиск тендеров
│   ├── matching/
│   │   └── smart_matcher.py   # Ранжирование
│   └── database/
│       └── models.py          # База данных фильтров
├── .env                        # Переменные окружения
├── main.py                     # Точка входа
└── requirements.txt            # Зависимости
```

### Ключевые файлы для работы:

1. **`src/parsers/zakupki_rss_parser.py`**
   - Метод `enrich_tender_from_page()` - обогащение данных
   - Метод `search_tenders_rss()` - поиск через RSS
   - Фильтрация по типу закупки

2. **`tender_sniper/instant_search.py`**
   - Метод `search_by_filter()` - главная логика поиска
   - Двойная фильтрация услуг
   - Генерация HTML отчетов

3. **`tender_sniper/matching/smart_matcher.py`**
   - Ранжирование тендеров
   - Проверка региона/типа

---

## 🔍 Логи и отладка

### Где смотреть логи:

**Railway (production):**
```
Railway Dashboard → tender-ai-bot → Deployments → View Logs
```

**Локально:**
```bash
# Запустить бота локально
python3 main.py

# Смотреть логи в реальном времени
tail -f logs/bot.log
```

### Важные логи для диагностики:

```
✓ Получено тендеров из RSS: X        # Сколько получено из RSS
⛔ Отфильтрован (услуга/работа)        # Фильтрация работает
📥 Загрузка полных данных тендеров... # Обогащение запустилось
🌐 Загружаем страницу тендера: ...    # Загрузка страницы
✅ Обогащено: цена=..., дедлайн=...   # Результаты обогащения
🎯 Совпадений (score ≥ 25): X         # Финальный результат
```

---

## 🎯 Приоритетные задачи

### 1. Исправить обогащение данных (ВЫСОКИЙ ПРИОРИТЕТ)
**Проблема:** Регион и срок подачи = Н/Д
**Решение:**
1. Добавить детальное логирование
2. Проверить что `enrich_tender_from_page` вызывается
3. Проверить что прокси работает
4. Проверить что паттерны извлечения корректны

### 2. Проверить фильтрацию услуг (СРЕДНИЙ ПРИОРИТЕТ)
**Проблема:** Медицинские услуги попадают в товары
**Решение:**
1. Проверить логи фильтрации
2. Убедиться что двойная проверка работает
3. Добавить больше индикаторов услуг

### 3. Оптимизация производительности (НИЗКИЙ ПРИОРИТЕТ)
- Кеширование страниц тендеров
- Batch обработка обогащения
- Уменьшить количество запросов к zakupki.gov.ru

---

## 🆘 Частые проблемы и решения

### "SSL Error" при загрузке страниц
```python
# Уже исправлено в коде:
self.session.verify = False
# Используется SOCKS5 прокси из .env
```

### "403 Forbidden" при git push
```bash
# В веб версии - это нормально (пушить только в claude/* ветки)
# В Desktop версии - должно работать в main
git push origin main
```

### Railway не деплоит автоматически
```bash
# Проверьте что изменения в main:
git log origin/main --oneline -3

# Вручную триггерните деплой в Railway UI:
Railway → Deployments → Redeploy
```

---

## 📞 Контакты и поддержка

**Разработчик:** Николай
**GitHub:** https://github.com/nikoly240702/tender-ai-bot
**Railway:** tender-ai-bot project

**Claude Code:**
- Веб: https://claude.ai
- Desktop: Локальная установка

---

## 📝 История изменений

### 01.12.2024
- ✅ Исправлено: `enrich_tender_from_page` теперь использует `self.session` с прокси
- ✅ Добавлено: Строгая фильтрация типов закупки (товары/услуги/работы)
- ✅ Добавлено: Фильтр тендеров старше 2 лет
- ✅ Добавлено: Двойная проверка типа перед scoring
- ✅ Добавлено: Подробное логирование обогащения
- ⚠️ Проблема: Обогащение не запускается (требуется диагностика)

### 27.11.2024
- ✅ Добавлена функция обогащения данных
- ✅ Парсинг адреса заказчика
- ✅ Извлечение НМЦК, дедлайна, региона

### 26.11.2024
- ✅ Переход на RSS парсинг (вместо HTML scraping)
- ✅ AI расширение поисковых запросов
- ✅ SmartMatcher для ранжирования

---

## 🚀 Быстрый старт с Claude Desktop

1. **Откройте проект:**
   ```bash
   cd ~/Desktop/"Агент по тендерам версия 01.12"
   claude .
   ```

2. **Скажите Claude:**
   ```
   Привет! Прочитай README_CLAUDE_TERMINAL.md и помоги исправить
   проблему с обогащением тендеров. Регион и дедлайн отображаются
   как Н/Д, хотя код исправлен.
   ```

3. **Claude автоматически:**
   - Проанализирует код
   - Добавит логи
   - Закоммитит изменения
   - Запушит в main
   - Railway задеплоит

**Готово!** Работайте с удовольствием! 🎉
