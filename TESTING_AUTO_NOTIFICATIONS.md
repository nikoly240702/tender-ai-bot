# 🧪 Тестирование автоматических уведомлений Tender Sniper

**Дата:** 2 декабря 2024
**Версия:** Агент 02.12

---

## 📋 Что такое автоматические уведомления?

**Tender Sniper** - это система real-time мониторинга новых тендеров, которая:
1. Каждые 5 минут проверяет zakupki.gov.ru на наличие новых тендеров
2. Сопоставляет их с вашими сохраненными фильтрами
3. Отправляет мгновенные уведомления в Telegram при совпадении

---

## ✅ Предварительная проверка

### 1. Проверьте конфигурацию

**Файл:** `config/features.yaml`

Убедитесь что включено:
```yaml
tender_sniper:
  enabled: true

  components:
    realtime_parser: true        # ← Должно быть true
    smart_matching: true         # ← Должно быть true
    instant_notifications: true  # ← Должно быть true
```

### 2. Проверьте что бот запущен

```bash
# Локально
cd ~/Desktop/"Агент 02.12"
python3 main.py

# Или проверьте Railway
# https://railway.app/project/YOUR_PROJECT_ID
```

### 3. Проверьте логи при старте

Должны быть строки:
```
🚀 ИНИЦИАЛИЗАЦИЯ TENDER SNIPER SERVICE
✅ Tender Sniper включен в конфигурации
🗄️  Инициализация базы данных...
📡 Инициализация Real-time Parser...
🎯 Инициализация Smart Matcher...
📱 Инициализация Telegram Notifier...
✅ ВСЕ КОМПОНЕНТЫ ИНИЦИАЛИЗИРОВАНЫ
🎯 ЗАПУСК МОНИТОРИНГА TENDER SNIPER
```

Если этих строк нет - **Tender Sniper не запущен!**

---

## 🧪 Способ 1: Создать фильтр и ждать (RECOMMENDED)

### Шаг 1: Создайте фильтр в боте

1. Откройте Telegram бота
2. Нажмите `/start`
3. Выберите **"Создать новый фильтр"**
4. Заполните критерии:
   - **Название:** Например "Компьютеры тест"
   - **Ключевые слова:** Например "компьютер" или "ноутбук"
   - **Регион:** Любой или конкретный
   - **Цена:** От 100 000 до 5 000 000
   - **Тип закупки:** Товары

### Шаг 2: Включите уведомления (если есть кнопка)

- В некоторых версиях есть кнопка "Включить уведомления"
- Если её нет - фильтр активен по умолчанию

### Шаг 3: Проверьте что фильтр сохранен

Должно появиться сообщение:
```
✅ Фильтр создан!

📋 Компьютеры тест
🔑 Ключевые слова: компьютер
💰 Цена: 100 000 - 5 000 000 руб.
📍 Регион: Все
📦 Тип: Товары

🔔 Вы будете получать уведомления о новых подходящих тендерах!
```

### Шаг 4: Ждите уведомлений

**Интервал проверки:** 5 минут (по умолчанию)

Уведомление придет в формате:
```
🔔 НОВЫЙ ПОДХОДЯЩИЙ ТЕНДЕР!

📋 Поставка компьютерной техники
💰 НМЦК: 2 500 000 руб.
📍 Москва
⏰ До подачи: 15.12.2024

🎯 Score: 85/100
✅ Совпадение с фильтром "Компьютеры тест"

[Открыть детали] [Анализировать]
```

---

## 🧪 Способ 2: Запустить тестовый скрипт (если существует)

### Проверьте наличие тестового скрипта

```bash
cd ~/Desktop/"Агент 02.12"
ls test_tender_sniper*.py
```

Если есть `test_tender_sniper_integration.py`:

```bash
python3 test_tender_sniper_integration.py
```

Этот скрипт должен:
1. Инициализировать все компоненты
2. Запустить один опрос
3. Показать найденные тендеры
4. Проверить работу матчинга

---

## 🧪 Способ 3: Ручная проверка компонентов

### 1. Проверка Real-time Parser

```bash
cd ~/Desktop/"Агент 02.12"
python3 -c "
import asyncio
from tender_sniper.parser import RealtimeParser

async def test():
    parser = RealtimeParser()

    async def callback(tenders):
        print(f'✅ Получено {len(tenders)} тендеров')
        for t in tenders[:3]:
            print(f'  - {t.get(\"name\", \"Без названия\")[:60]}')

    parser.add_callback(callback)

    # Один опрос
    await parser.start(keywords='компьютер', max_tenders_per_poll=10)

asyncio.run(test())
"
```

### 2. Проверка Smart Matcher

```bash
python3 -c "
from tender_sniper.matching import SmartMatcher

matcher = SmartMatcher()

tender = {
    'number': '0123456789012345678',
    'name': 'Поставка компьютерной техники',
    'price': 2500000,
    'region': 'Москва'
}

filter_data = {
    'id': 1,
    'name': 'Тест',
    'keywords': ['компьютер', 'ноутбук'],
    'price_min': 100000,
    'price_max': 5000000,
    'regions': []
}

result = matcher.match_tender(tender, filter_data)
print(f'✅ Score: {result[\"score\"]}')
print(f'✅ Reasons: {result[\"reasons\"]}')
"
```

### 3. Проверка уведомлений

```bash
python3 -c "
import asyncio
import os
from tender_sniper.notifications.telegram_notifier import TelegramNotifier

async def test():
    bot_token = os.getenv('TELEGRAM_BOT_TOKEN')
    notifier = TelegramNotifier(bot_token)

    # Замените YOUR_CHAT_ID на ваш ID
    YOUR_CHAT_ID = 123456789

    tender = {
        'number': '0123456789012345678',
        'name': 'Тестовый тендер',
        'price': 1000000,
        'url': 'https://zakupki.gov.ru/...'
    }

    filter_name = 'Тестовый фильтр'

    await notifier.send_match_notification(
        chat_id=YOUR_CHAT_ID,
        tender=tender,
        filter_name=filter_name,
        score=85
    )

    print('✅ Уведомление отправлено!')
    await notifier.close()

asyncio.run(test())
"
```

---

## 🔍 Что проверять в логах

### Успешный мониторинг:

```
📡 Опрос #1 (11:00:00)
   ✅ Получено тендеров: 100
   🆕 Новых тендеров: 15
   📢 Вызов 1 callback(s)...

🔄 Обработка 15 новых тендеров...
   📋 Активных фильтров: 3

   Фильтр #1: "Компьютеры"
      ✅ Найдено совпадений: 2
      📤 Отправка уведомлений...

   Фильтр #2: "Медтехника"
      ℹ️  Совпадений не найдено

✅ Обработка завершена
⏳ Следующий опрос через 300 сек...
```

### Если уведомления не приходят:

**Проблема 1:** Real-time Parser не запущен
```
# НЕТ строк типа:
📡 Опрос #1 (11:00:00)
```
**Решение:** Проверьте `config/features.yaml` → `tender_sniper.components.realtime_parser: true`

**Проблема 2:** Фильтры не созданы
```
🔄 Обработка 15 новых тендеров...
   📋 Активных фильтров: 0  ← ПРОБЛЕМА!
```
**Решение:** Создайте фильтр через бота

**Проблема 3:** Нет совпадений
```
   Фильтр #1: "Компьютеры"
      ℹ️  Совпадений не найдено  ← Критерии слишком строгие
```
**Решение:** Расширьте критерии (добавьте ключевые слова, увеличьте диапазон цены)

**Проблема 4:** Ошибка отправки уведомления
```
❌ Ошибка отправки уведомления: Chat not found
```
**Решение:** Проверьте что пользователь активировал бота (`/start`)

---

## 📊 Как узнать свой Chat ID

### Способ 1: Через бота

1. Напишите боту `/start`
2. В логах появится:
```
INFO - Пользователь 123456789 запустил бота
                ^^^^^^^^^ ваш chat_id
```

### Способ 2: Через @userinfobot

1. Найдите бота `@userinfobot` в Telegram
2. Отправьте `/start`
3. Скопируйте `Id: 123456789`

---

## 🎯 Ожидаемые результаты

### ✅ Успешный тест:

1. Фильтр создан в боте
2. В логах видны опросы каждые 5 минут
3. При появлении подходящего тендера приходит уведомление
4. Кнопки в уведомлении работают

### ❌ Если что-то не работает:

1. Проверьте `config/features.yaml`
2. Проверьте что бот запущен
3. Проверьте логи на наличие ошибок
4. Попробуйте перезапустить бота

---

## 💡 Советы по тестированию

### 1. Используйте широкие критерии

Для теста создайте фильтр:
- Ключевые слова: "поставка" или "услуги"
- Цена: от 0 до 100 000 000
- Регион: Все
- Тип: Любой

Так вы гарантированно получите совпадения.

### 2. Уменьшите интервал опроса (для теста)

Отредактируйте `tender_sniper/service.py`:
```python
poll_interval: int = 60  # Было 300, стало 60 (1 минута)
```

**ВАЖНО:** После теста верните обратно на 300!

### 3. Следите за логами в реальном времени

```bash
# Локально
python3 main.py

# На Railway
railway logs --follow
```

---

## 🐛 Troubleshooting

### Tender Sniper не запускается

**Симптом:**
```
❌ Tender Sniper отключен в config/features.yaml
```

**Решение:**
```bash
cd ~/Desktop/"Агент 02.12"
nano config/features.yaml
# Измените tender_sniper.enabled: false → true
```

### Нет новых тендеров

**Симптом:**
```
📡 Опрос #5 (11:20:00)
   ✅ Получено тендеров: 100
   ℹ️  Все тендеры уже были обработаны ранее
```

**Это нормально!** Новые тендеры публикуются не каждые 5 минут.

### Ошибка прокси

**Симптом:**
```
❌ Ошибка получения RSS: ProxyError
```

**Решение:**
1. Проверьте `.env` → `PROXY_URL`
2. Проверьте что прокси работает:
   ```bash
   curl -x socks5h://user:pass@host:port https://zakupki.gov.ru
   ```

---

## 📄 Связанные документы

- `PROJECT_STATUS.md` - Текущий статус проекта
- `CRITICAL_FIX_02.12.2024.md` - Критическое исправление обогащения
- `README_CLAUDE_TERMINAL.md` - Инструкции для работы через Claude

---

**Удачи с тестированием!** 🚀

Если столкнетесь с проблемами - проверьте логи и создайте issue с детальным описанием.
