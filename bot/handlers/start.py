"""
Обработчики команды /start и главного меню.
"""

from aiogram import Router, F
from aiogram.filters import CommandStart, Command
from aiogram.types import Message
from aiogram.fsm.context import FSMContext

from bot.keyboards import get_main_menu_keyboard
from bot.states import SearchStates

router = Router()


@router.message(CommandStart())
async def cmd_start(message: Message, state: FSMContext):
    """
    Обработчик команды /start.
    Приветствует пользователя и показывает главное меню.
    """
    # Очищаем любое предыдущее состояние
    await state.clear()

    welcome_text = (
        "👋 <b>Добро пожаловать в TenderBot!</b>\n\n"
        "Я помогу вам найти и проанализировать тендеры на zakupki.gov.ru\n\n"
        "<b>Что я умею:</b>\n"
        "🔍 Умный поиск тендеров с расширением запросов через AI\n"
        "📄 Автоматическое скачивание документации\n"
        "🤖 AI-анализ документов тендера\n"
        "📊 Выявление пробелов в документации\n"
        "❓ Генерация вопросов для заказчика\n"
        "📂 Сохранение истории поисков\n\n"
        "<i>Выберите действие в меню ниже:</i>"
    )

    await message.answer(
        welcome_text,
        reply_markup=get_main_menu_keyboard(),
        parse_mode="HTML"
    )


@router.message(Command("help"))
@router.message(F.text == "ℹ️ Помощь")
async def cmd_help(message: Message):
    """Обработчик команды /help и кнопки "Помощь"."""

    help_text = (
        "<b>📖 Как пользоваться ботом:</b>\n\n"
        "<b>1. Новый поиск</b>\n"
        "• Введите поисковый запрос (например, \"поставка бумаги\")\n"
        "• Выберите ценовой диапазон или укажите свой\n"
        "• Выберите количество тендеров для поиска\n"
        "• Получите результаты с кратким описанием\n\n"
        "<b>2. Просмотр результатов</b>\n"
        "• Нажмите \"Подробнее\" для просмотра деталей тендера\n"
        "• Нажмите \"Анализировать\" для AI-анализа документов\n"
        "• Получите HTML-отчет с полным анализом\n\n"
        "<b>3. История поисков</b>\n"
        "• Просмотрите ваши предыдущие поиски\n"
        "• Повторите поиск одной кнопкой\n\n"
        "<b>💡 Советы:</b>\n"
        "• Используйте конкретные запросы (\"поставка компьютеров\" вместо \"компьютеры\")\n"
        "• AI-анализ занимает 1-2 минуты на тендер\n"
        "• Анализируйте только интересные тендеры для экономии времени\n\n"
        "<i>Вопросы? Напишите /start для возврата в главное меню</i>"
    )

    await message.answer(help_text, parse_mode="HTML")


@router.message(F.text == "🔍 Новый поиск")
async def start_new_search(message: Message, state: FSMContext):
    """
    Начало нового поиска.
    Переводит пользователя в состояние ожидания поискового запроса.
    """
    await state.clear()
    await state.set_state(SearchStates.waiting_for_query)

    await message.answer(
        "🔍 <b>Шаг 1 из 3: Поисковый запрос</b>\n\n"
        "Введите, что вы ищете:\n\n"
        "Примеры:\n"
        "• поставка офисной бумаги\n"
        "• компьютерное оборудование\n"
        "• ремонт помещений\n"
        "• услуги уборки\n\n"
        "<i>💡 AI автоматически расширит ваш запрос синонимами и смежными терминами</i>",
        parse_mode="HTML"
    )


# История поисков теперь обрабатывается в bot/handlers/history.py
