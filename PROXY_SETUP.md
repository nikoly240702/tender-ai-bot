# Настройка прокси для доступа к zakupki.gov.ru

Если бот развернут за пределами России (например, на Railway), zakupki.gov.ru может блокировать запросы. Для решения этой проблемы можно использовать российский прокси-сервер.

## Зачем нужен прокси?

zakupki.gov.ru может блокировать запросы с иностранных IP-адресов. Прокси позволяет боту делать запросы через российский IP-адрес.

## Способы получения прокси

### 1. Бесплатные прокси (для теста)

**Сайты с бесплатными прокси:**
- https://free-proxy-list.net/ (фильтр: Russia, HTTPS)
- https://www.proxyscrape.com/free-proxy-list (фильтр: Russia)

**Недостатки:**
- Нестабильные
- Медленные
- Могут не работать с zakupki.gov.ru
- Часто отваливаются

### 2. Платные прокси (рекомендуется)

#### Proxy6.net (Россия)
- **Сайт**: https://proxy6.net/
- **Цена**: от 100₽/месяц за 1 прокси
- **Преимущества**: Российская компания, стабильные прокси, хорошая поддержка

**Как купить:**
1. Зарегистрируйтесь на https://proxy6.net/
2. Выберите "IPv4 прокси" → "Россия"
3. Выберите количество (1 прокси достаточно)
4. Оплатите
5. Скопируйте данные прокси в формате `IP:PORT:USER:PASS`

#### Proxys.io
- **Сайт**: https://proxys.io/
- **Цена**: от $1.5/месяц
- **Преимущества**: Дешево, много локаций

#### Webshare.io
- **Сайт**: https://www.webshare.io/
- **Цена**: 10 бесплатных прокси, далее от $1.99/месяц
- **Преимущества**: Есть бесплатный tier

## Настройка прокси в боте

### Шаг 1: Получите данные прокси

Прокси может быть в одном из форматов:
- `IP:PORT` (без авторизации)
- `IP:PORT:USER:PASS` (с авторизацией)
- `http://IP:PORT` (готовый URL)
- `http://USER:PASS@IP:PORT` (готовый URL с авторизацией)

### Шаг 2: Добавьте в переменные окружения

#### Локально (.env файл)

Отредактируйте файл `.env`:

```env
# Без авторизации
PROXY_URL=http://185.123.45.67:8080

# С авторизацией
PROXY_URL=http://username:password@185.123.45.67:8080
```

#### На Railway

1. Зайдите в Railway Dashboard
2. Откройте ваш проект
3. Выберите сервис (tender-ai-bot)
4. Перейдите на вкладку **Variables**
5. Нажмите **+ New Variable**
6. Добавьте:
   - **Key**: `PROXY_URL`
   - **Value**: `http://username:password@185.123.45.67:8080`
7. Railway автоматически перезапустит деплой

### Шаг 3: Проверка

После настройки прокси:
1. Перезапустите бота (Railway сделает это автоматически)
2. Сделайте тестовый запрос через Telegram
3. Проверьте логи - должна появиться строка: `🔐 Используется прокси: 185.123.45.67:8080`

## Форматы прокси URL

Бот поддерживает следующие форматы:

```env
# HTTP прокси без авторизации
PROXY_URL=http://185.123.45.67:8080

# HTTP прокси с авторизацией
PROXY_URL=http://username:password@185.123.45.67:8080

# HTTPS прокси
PROXY_URL=https://185.123.45.67:8080

# SOCKS5 прокси (требует установки pysocks)
PROXY_URL=socks5://username:password@185.123.45.67:1080
```

## Troubleshooting

### Прокси не работает

**Проверьте логи:**
```
Railway Dashboard → Logs
```

**Ошибка**: `ProxyError` или `Connection timeout`
**Решение**:
- Проверьте правильность данных прокси
- Убедитесь, что прокси работает (проверьте в браузере или через curl)
- Попробуйте другой прокси

### Прокси медленный

**Симптомы**: Бот долго отвечает на запросы

**Решение**:
- Купите более быстрый прокси
- Выберите прокси с меньшим пингом
- Используйте приватный (не публичный) прокси

### Прокси блокируется zakupki.gov.ru

**Симптомы**: `Connection timeout` или `403 Forbidden`

**Решение**:
- Используйте residential прокси (а не datacenter)
- Смените прокси на другой
- Уменьшите частоту запросов (увеличьте `delay` в парсере)

## Проверка прокси вручную

### Через curl

```bash
# Без авторизации
curl -x http://185.123.45.67:8080 https://zakupki.gov.ru

# С авторизацией
curl -x http://username:password@185.123.45.67:8080 https://zakupki.gov.ru
```

### Через Python

```python
import requests

proxies = {
    'http': 'http://username:password@185.123.45.67:8080',
    'https': 'http://username:password@185.123.45.67:8080'
}

response = requests.get('https://zakupki.gov.ru', proxies=proxies, timeout=10)
print(f"Status: {response.status_code}")
```

## Рекомендации

1. **Используйте платные прокси** для продакшена (стабильнее)
2. **Храните credentials в безопасности** - не коммитьте в git
3. **Ротация прокси**: если делаете много запросов, используйте несколько прокси
4. **Мониторинг**: следите за логами Railway - если прокси падает, вы увидите ошибки

## Альтернативы прокси

Если прокси не подходит, рассмотрите:

1. **VPS в России**: Развернуть бота на Selectel, Timeweb или Yandex Cloud
2. **Официальное API zakupki.gov.ru**: Более надежно, но требует регистрации
3. **VPN**: Настроить VPN на Railway (сложнее, но возможно)

## Стоимость

Примерная стоимость прокси на месяц:

| Провайдер | Тип | Цена |
|-----------|-----|------|
| Proxy6.net | Shared | 100-300₽ |
| Proxy6.net | Private | 300-600₽ |
| Proxys.io | Datacenter | $1.5-5 |
| Webshare.io | Datacenter | Бесплатно (10 шт) |

**Итого:** ~$2-7/месяц за стабильный прокси

## Поддержка

Если возникли проблемы с настройкой прокси:
1. Проверьте логи Railway
2. Убедитесь, что прокси работает (проверьте через curl)
3. Создайте Issue в репозитории с описанием проблемы
