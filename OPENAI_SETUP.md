# Настройка OpenAI API

## Пошаговая инструкция

### 1. Регистрация на OpenAI

1. Откройте https://platform.openai.com/
2. Нажмите **"Sign up"** (или войдите, если уже есть аккаунт)
3. Зарегистрируйтесь через:
   - Email
   - Google аккаунт
   - Microsoft аккаунт
   - Apple ID

### 2. Добавление платежного метода

**ВАЖНО:** OpenAI требует добавить платежный метод, но:
- Новым пользователям дают **$5 бесплатных кредитов**
- GPT-4o-mini очень дешевая: **~$0.15 за 1 миллион токенов**
- Один анализ тендера ≈ $0.01-0.05
- $5 хватит на **100-500 анализов**

**Как добавить:**
1. Перейдите в https://platform.openai.com/settings/organization/billing/overview
2. Нажмите **"Add payment method"**
3. Введите данные карты (поддерживаются российские карты Visa/Mastercard)
4. Установите лимит расходов (рекомендую $10-20/месяц)

### 3. Создание API ключа

1. Перейдите в https://platform.openai.com/api-keys
2. Нажмите **"+ Create new secret key"**
3. Введите имя ключа (например, "Tender Analysis")
4. Выберите права:
   - **Permissions:** All (или только Read)
   - Можно оставить по умолчанию
5. Нажмите **"Create secret key"**
6. **ВАЖНО:** Скопируйте ключ СЕЙЧАС! Он показывается только один раз!
   - Ключ выглядит так: `sk-proj-abcd1234...`

### 4. Добавление ключа в проект

Откройте файл `.env` в корне проекта и замените:

```bash
OPENAI_API_KEY=your-openai-api-key-here
```

На ваш ключ:

```bash
OPENAI_API_KEY=sk-proj-ваш_настоящий_ключ_здесь
```

**Сохраните файл!**

### 5. Проверка работы

Запустите тестовую проверку:

```bash
cd ~/tender-ai-agent
python3 -c "
from src.utils.config_loader import ConfigLoader
from src.analyzers.llm_adapter import LLMFactory

config = ConfigLoader()
llm_config = config.get_llm_config()
print(f'Провайдер: {llm_config[\"provider\"]}')
print(f'Модель: {llm_config.get(\"model\", \"авто\")}')

# Тестовый запрос
adapter = LLMFactory.create(
    provider=llm_config['provider'],
    api_key=llm_config.get('api_key'),
    model=llm_config.get('model')
)
response = adapter.generate('You are a helpful assistant', 'Say hello in Russian')
print(f'Ответ: {response}')
"
```

Если все работает, увидите ответ на русском языке!

### 6. Запуск анализа тендера

Теперь можно анализировать тендеры:

```bash
python3 main.py путь/к/документу.pdf
```

## Стоимость использования

### Модели OpenAI:

| Модель | Цена за вход | Цена за выход | Рекомендация |
|--------|--------------|---------------|--------------|
| **gpt-4o-mini** | $0.15/1M | $0.60/1M | ⭐ Рекомендуется |
| gpt-4o | $2.50/1M | $10.00/1M | Для сложных задач |
| gpt-4-turbo | $10.00/1M | $30.00/1M | Дорого |
| gpt-3.5-turbo | $0.50/1M | $1.50/1M | Устаревшая |

### Примерный расход на один анализ тендера:

**С моделью gpt-4o-mini (рекомендуется):**
- Входные токены: ~10,000-20,000 (документация + промпты)
- Выходные токены: ~2,000-4,000 (анализ)
- **Стоимость: $0.002-0.005 за тендер** (примерно 0.2-0.5 рубля)

**Итого:**
- $5 бесплатных кредитов = **1,000-2,500 анализов тендеров**
- $10/месяц = **2,000-5,000 анализов**

### Как экономить:

1. **Используйте gpt-4o-mini** (по умолчанию) - в 15 раз дешевле GPT-4
2. **Анализируйте только нужные файлы** - не загружайте лишние документы
3. **Установите лимит расходов** в настройках billing
4. **Следите за использованием:** https://platform.openai.com/usage

## Переключение на другую модель

Если хотите использовать GPT-4 (дороже, но лучше качество):

1. Отредактируйте `config/settings.yaml`:
```yaml
llm:
  provider: "openai"
  model: "gpt-4o"  # Или "gpt-4-turbo"
```

2. Перезапустите анализ

## Безопасность

- ❌ **НЕ ПУБЛИКУЙТЕ** файл `.env` в Git (он уже в .gitignore)
- ❌ **НЕ ДЕЛИТЕСЬ** API ключом
- ✅ Если ключ утек - удалите его в настройках OpenAI и создайте новый
- ✅ Установите лимиты расходов
- ✅ Регулярно проверяйте использование

## Частые проблемы

### Ошибка: "Insufficient credits"
→ Добавьте средства на аккаунт: https://platform.openai.com/settings/organization/billing/overview

### Ошибка: "Invalid API key"
→ Проверьте, что ключ правильно скопирован в `.env` (должен начинаться с `sk-proj-`)

### Ошибка: "Rate limit exceeded"
→ Вы превысили лимит запросов. Подождите минуту или увеличьте лимит в настройках.

### Ошибка: "The model does not exist"
→ Проверьте название модели в `config/settings.yaml`

### Высокий расход средств
→ Используйте `gpt-4o-mini` вместо `gpt-4`
→ Установите лимиты расходов в billing settings

## Альтернативы

Если OpenAI не подходит:

1. **Groq** - бесплатно, см. `LLM_PROVIDERS.md`
2. **Google Gemini** - бесплатный tier
3. **Ollama** - локально, без интернета

## Поддержка

- Документация OpenAI: https://platform.openai.com/docs/
- Цены: https://openai.com/api/pricing/
- Статус системы: https://status.openai.com/
- Поддержка: https://help.openai.com/

---

**Готово! Теперь можно анализировать тендеры с OpenAI GPT!** 🎉
