# Руководство по интегрированной системе поиска и анализа тендеров

## Обзор системы

Интегрированная система объединяет все компоненты для полного цикла работы с тендерами:

1. **Умный поиск тендеров** - AI расширяет поисковый запрос для лучшего охвата
2. **Автоматическое скачивание документов** - извлечение файлов из карточек тендеров
3. **Анализ документов через AI** - полный анализ тендерной документации
4. **Генерация отчетов** - красивые HTML отчеты с результатами

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│               ИНТЕГРИРОВАННАЯ СИСТЕМА                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. SmartSearchExpander (AI)                                │
│     └─> Расширяет запрос: "ноутбуки", "мониторы"...        │
│                                                              │
│  2. ZakupkiEnhancedParser                                   │
│     └─> Ищет тендеры через RSS zakupki.gov.ru              │
│     └─> Извлекает: цена, регион, сроки, тип заказчика      │
│                                                              │
│  3. ZakupkiDocumentDownloader                               │
│     └─> Парсит карточку тендера                             │
│     └─> Скачивает: ТЗ, договоры, спецификации              │
│                                                              │
│  4. TenderAnalyzer (AI)                                     │
│     └─> Анализирует документы                               │
│     └─> Оценивает соответствие профилю компании            │
│                                                              │
│  5. ReportGenerator                                         │
│     └─> Создает HTML отчеты                                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Новые возможности

### 1. Извлечение дополнительных данных

Теперь система извлекает:
- ⏰ **Срок подачи заявок** - точная дата окончания приема заявок
- 🏆 **Срок определения победителя** - дата подведения итогов
- 📍 **Регион** - автоматическое определение региона
- 🏛️ **Тип заказчика** - федеральный/региональный/муниципальный
- 💳 **Условия оплаты** - информация об авансе, рассрочке
- 📦 **Количество товаров** - если указано в карточке

### 2. Входные параметры в отчетах

HTML отчеты теперь показывают:
- 🔍 Оригинальный поисковый запрос
- 💡 Расширенные AI запросы
- 💰 Диапазон цен (от-до)
- 📍 Регионы поставки (если указаны)
- ⏱️ Время выполнения поиска

### 3. Автоматическое скачивание документов

Модуль `ZakupkiDocumentDownloader`:
- Парсит карточку тендера на zakupki.gov.ru
- Находит все прикрепленные документы
- Классифицирует по типам:
  - `tech_spec` - техническое задание
  - `contract` - проект контракта
  - `notice` - извещение
  - `specification` - спецификация
  - `other` - прочие документы
- Скачивает файлы в структурированные папки

## Использование

### Базовый пример

```python
from integrated_tender_system import IntegratedTenderSystem

# Создаем систему
system = IntegratedTenderSystem()

# Запускаем полный цикл
result = system.search_and_analyze(
    search_query="компьютерное оборудование",
    price_min=500000,
    price_max=3000000,
    max_tenders=5,
    regions=['Москва', 'Санкт-Петербург'],
    download_documents=True,
    analyze_documents=True
)
```

### Только поиск (без анализа)

```python
result = system.search_and_analyze(
    search_query="программное обеспечение",
    price_min=100000,
    price_max=1000000,
    max_tenders=10,
    download_documents=False,  # Не скачивать
    analyze_documents=False     # Не анализировать
)
```

### Скачивание + Анализ

```python
result = system.search_and_analyze(
    search_query="канцелярские товары",
    price_min=50000,
    price_max=500000,
    max_tenders=3,
    download_documents=True,   # Скачать документы
    analyze_documents=True     # Проанализировать через AI
)
```

## Запуск готовых скриптов

### 1. Автоматический тест

```bash
python3 test_auto.py
```

Что делает:
- Ищет 3 тендера по запросу "компьютерное оборудование"
- Извлекает документы из карточек
- Сохраняет результаты в JSON

### 2. Интерактивный тест

```bash
python3 test_integrated_system.py
```

Что делает:
- Ищет тендеры
- Спрашивает, скачивать ли документы
- Позволяет контролировать процесс

### 3. Полная интеграция

```bash
python3 integrated_tender_system.py
```

Что делает:
- Полный цикл: поиск → скачивание → анализ
- Генерирует HTML отчет
- Открывает отчет в браузере

## Структура результатов

```python
{
    'search_params': {
        'query': 'компьютерное оборудование',
        'expanded_queries': ['ноутбуки', 'мониторы', ...],
        'price_min': 500000,
        'price_max': 3000000,
        'regions': ['Москва'],
        'time': 12.5  # секунд
    },
    'tenders_found': 10,
    'tenders_analyzed': 5,
    'results': [
        {
            'tender_info': {
                'number': '0123456789',
                'name': 'Поставка компьютерного оборудования',
                'price': 1500000,
                'price_formatted': '1,500,000.00 ₽',
                'region': 'Москва',
                'customer_type': 'Федеральный',
                'submission_deadline': '15.12.2025 18:00',
                'winner_determination_date': '20.12.2025',
                'law': '44-ФЗ',
                ...
            },
            'documents_downloaded': [
                {
                    'filename': 'tech_spec.pdf',
                    'path': '/path/to/file',
                    'type': 'tech_spec',
                    'size_kb': 250.5
                },
                ...
            ],
            'analysis_result': {
                'analysis_summary': {
                    'is_suitable': True,
                    'confidence_score': 85.5,
                    'summary_text': '...'
                },
                ...
            }
        }
    ]
}
```

## Создание HTML отчетов

Отчеты автоматически сохраняются в `output/reports/` и включают:

### Заголовок отчета
- Название: "Умный поиск тендеров"
- Бадж "AI-Powered"
- Входные параметры поиска
- Расширенные запросы

### Статистика
- Количество найденных тендеров
- Количество поисковых запросов
- Время выполнения

### Карточки тендеров
Каждая карточка содержит:
- 📋 Номер тендера
- 📝 Полное название
- 💰 Цена контракта
- 🏢 Заказчик
- 📍 Регион
- 🏛️ Тип заказчика
- ⏰ **Срок подачи заявок** ← НОВОЕ
- 🏆 **Определение победителя** ← НОВОЕ
- 🏷️ Коды ОКПД2
- 🔖 ИКЗ
- 📜 Закон (44-ФЗ/223-ФЗ)
- 🔖 Тип процедуры
- ⏱️ Этап размещения
- 💳 Условия оплаты (если есть)
- 📦 Количество товаров (если есть)

### Кнопка перехода
- Прямая ссылка на zakupki.gov.ru

## Примеры использования

### Пример 1: Поиск тендеров на IT-оборудование

```python
system = IntegratedTenderSystem()

result = system.search_and_analyze(
    search_query="серверное оборудование",
    price_min=1000000,
    price_max=10000000,
    max_tenders=10,
    regions=['Москва', 'Московская область'],
    download_documents=True,
    analyze_documents=True
)

# Результат:
# - Найдено 10 тендеров
# - AI расширил запрос: "серверы", "системы хранения данных", "СХД"
# - Скачано 30 документов
# - Проанализировано 10 тендеров
# - Подходит: 7 тендеров
```

### Пример 2: Быстрый поиск без анализа

```python
result = system.search_and_analyze(
    search_query="офисная мебель",
    price_min=100000,
    price_max=1000000,
    max_tenders=20,
    download_documents=False,
    analyze_documents=False
)

# Результат:
# - Найдено 20 тендеров за 5 секунд
# - Только базовая информация (без скачивания и анализа)
```

## Файлы и модули

### Основные компоненты

| Файл | Описание |
|------|----------|
| `integrated_tender_system.py` | Главный класс интегрированной системы |
| `src/parsers/smart_search_expander.py` | AI расширение поисковых запросов |
| `src/parsers/zakupki_enhanced_parser.py` | Улучшенный парсер с детальным извлечением |
| `src/parsers/zakupki_document_downloader.py` | Загрузчик документов из карточек |
| `smart_tender_search.py` | Умный поиск с HTML отчетами |

### Тестовые скрипты

| Файл | Описание |
|------|----------|
| `test_auto.py` | Автоматический тест без интерактивного ввода |
| `test_integrated_system.py` | Интерактивный тест с подтверждениями |

## Конфигурация

Все настройки в `config/settings.json`:

```json
{
  "llm": {
    "provider": "groq",
    "api_key": "your-api-key",
    "model_fast": "llama-3.3-70b-versatile"
  },
  "search": {
    "default_price_min": 100000,
    "default_price_max": 10000000,
    "max_results_per_query": 50
  }
}
```

## Ограничения и рекомендации

### Технические ограничения

1. **Rate Limits zakupki.gov.ru**
   - Не более 10 запросов в минуту к RSS
   - Пауза 0.5 сек между скачиванием документов

2. **Доступность документов**
   - Некоторые тендеры требуют авторизации
   - Часть документов может быть в защищенном разделе
   - Excel файлы с посещаемостью не всегда содержат тендерные документы

3. **AI анализ**
   - Требуется API ключ (Groq, OpenAI, Anthropic)
   - Большие документы обрабатываются дольше
   - Рекомендуется ограничивать `max_tenders` для полного анализа

### Рекомендации

✅ **Для быстрого поиска:**
- Используйте `analyze_documents=False`
- Увеличьте `max_tenders` до 20-50
- Отключите скачивание документов

✅ **Для глубокого анализа:**
- Ограничьте `max_tenders` до 3-5
- Включите `analyze_documents=True`
- Убедитесь в наличии API ключа

✅ **Для мониторинга:**
- Запускайте скрипт по расписанию (cron)
- Сохраняйте результаты в БД
- Настройте уведомления о подходящих тендерах

## Решение проблем

### Не находит документы в карточке тендера

**Причины:**
- Документы находятся в защищенном разделе (требуется авторизация)
- Карточка содержит только служебные файлы (отчеты о посещаемости)
- Тендер еще не опубликован полностью

**Решение:**
- Проверьте карточку тендера вручную в браузере
- Для защищенных разделов потребуется авторизация (будущая доработка)

### Не извлекает сроки подачи/определения победителя

**Причины:**
- RSS фид содержит ограниченную информацию
- Формат данных в RSS может меняться

**Решение:**
- Эти поля будут заполнены при парсинге полной карточки тендера
- В текущей версии используются базовые regex паттерны

### Ошибка SSL Certificate

**Решение:**
```python
# Уже реализовано в коде
import urllib3
urllib3.disable_warnings()
# В requests используется verify=False
```

## Следующие шаги развития

### Краткосрочные (1-2 недели)

- [ ] Добавить поддержку авторизации на zakupki.gov.ru
- [ ] Улучшить извлечение сроков из полной карточки тендера
- [ ] Добавить фильтрацию по регионам в RSS запросе
- [ ] Кэширование результатов поиска
- [ ] Экспорт результатов в Excel

### Среднесрочные (1-2 месяца)

- [ ] База данных для хранения истории тендеров
- [ ] Веб-интерфейс для управления поиском
- [ ] Настраиваемые уведомления (email, Telegram)
- [ ] Автоматический мониторинг новых тендеров
- [ ] API для интеграции с другими системами

### Долгосрочные (3-6 месяцев)

- [ ] Машинное обучение для улучшения соответствия профилю
- [ ] Анализ конкурентов по тендерам
- [ ] Прогнозирование шансов на победу
- [ ] Автоматическая генерация коммерческих предложений
- [ ] Интеграция с CRM системами

## Поддержка

Вопросы и предложения:
- Создайте issue в репозитории
- Проверьте документацию в `docs/`
- Изучите примеры в `examples/`

## Лицензия

MIT License - см. файл LICENSE

---

**Версия:** 2.0
**Дата обновления:** 2025-01-12
**Статус:** Production Ready ✅
