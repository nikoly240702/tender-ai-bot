# 🔍 Настройка Sentry для мониторинга ошибок

## Что такое Sentry?

Sentry - это система отслеживания ошибок и мониторинга производительности, которая помогает:
- 📊 Отслеживать все ошибки в реальном времени
- 🔔 Получать уведомления о критических проблемах
- 📈 Анализировать производительность приложения
- 🐛 Быстро находить и исправлять баги

## Установка

```bash
pip install sentry-sdk
```

## Получение Sentry DSN

1. Зарегистрируйтесь на https://sentry.io/signup/ (бесплатный план доступен)
2. Создайте новый проект:
   - Project type: Python
   - Alert frequency: можно оставить по умолчанию
3. Скопируйте DSN (Data Source Name) из настроек проекта
   - Формат: `https://xxxxx@o123456.ingest.sentry.io/7890123`

## Настройка бота

Добавьте в файл `.env`:

```bash
# Sentry DSN для мониторинга ошибок
SENTRY_DSN=https://xxxxx@o123456.ingest.sentry.io/7890123
```

## Проверка работы

После запуска бота вы должны увидеть в логах:

```
✅ Sentry мониторинг активирован
```

Если DSN не указан:

```
ℹ️  Sentry мониторинг отключен (SENTRY_DSN не указан)
```

## Что отслеживается?

### Автоматически

- ❌ Все неперехваченные исключения
- 📝 Логи уровня ERROR и выше
- 🔄 HTTP запросы (через aiohttp)
- ⏱️ Производительность (10% запросов)

### Вручную (в коде)

```python
from tender_sniper.monitoring import capture_exception, capture_message

try:
    # Ваш код
    pass
except Exception as e:
    capture_exception(
        e,
        level="error",
        extra={"user_id": user_id, "filter_id": filter_id},
        tags={"component": "search"}
    )
```

## Настройка уведомлений

1. Зайдите в Sentry Dashboard → Settings → Alerts
2. Создайте новое правило (Alert Rule):
   - **Trigger:** When an event occurs
   - **Conditions:**
     - Error level: error, fatal
     - Frequency: более 1 раза за 1 минуту
   - **Actions:**
     - Send notification via Email/Slack/Telegram

## Интеграции

### Telegram

Для получения уведомлений в Telegram:

1. Settings → Integrations → Telegram
2. Подключите Telegram бота Sentry
3. Настройте правила уведомлений

### Slack

1. Settings → Integrations → Slack
2. Выберите канал для уведомлений
3. Настройте фильтры событий

## Фильтрация данных

Система автоматически фильтрует чувствительные данные:

- 🔐 Токены (token, api_key)
- 🔑 Пароли (password, secret)
- 📧 Email адреса
- 📞 Номера телефонов

## Производительность

Настройки трассировки (в `bot/main.py`):

```python
init_sentry(
    environment="production",
    traces_sample_rate=0.1,  # 10% запросов - балан между мониторингом и нагрузкой
    profiles_sample_rate=0.1  # 10% профилирования
)
```

Для development окружения можно увеличить до 1.0 (100%).

## Troubleshooting

### Sentry не отправляет события

1. Проверьте DSN в .env файле
2. Проверьте интернет-соединение
3. Проверьте логи на наличие ошибок инициализации
4. Убедитесь, что `sentry-sdk` установлен: `pip show sentry-sdk`

### Слишком много событий

Увеличьте порог отправки ошибок в `monitoring.py`:

```python
logging_integration = LoggingIntegration(
    level=logging.WARNING,     # Только WARNING и выше
    event_level=logging.ERROR  # События только для ERROR
)
```

### Высокая нагрузка

Уменьшите sample rate:

```python
traces_sample_rate=0.01,  # 1% вместо 10%
```

## Полезные ссылки

- [Sentry Documentation](https://docs.sentry.io/platforms/python/)
- [Best Practices](https://docs.sentry.io/platforms/python/best-practices/)
- [Filtering Data](https://docs.sentry.io/platforms/python/data-management/sensitive-data/)

## Стоимость

- **Free Plan**: 5,000 событий/месяц, 1 пользователь
- **Team Plan**: $26/месяц - 50,000 событий/месяц
- **Business Plan**: от $80/месяц - unlimited events

Для большинства ботов достаточно Free плана.

---

**Автор:** Tender AI Bot Team
**Дата:** 05.12.2024
