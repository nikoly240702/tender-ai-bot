# 📋 Данные для доработок по боту от 10.12.2024

## ✅ КРИТИЧНЫЕ ПРОБЛЕМЫ (ИСПРАВЛЕНО)

### ✓ 1. Обработка ошибок в автомониторинге
**Статус:** ✅ ИСПРАВЛЕНО

**Что сделано:**
- Добавлен счетчик последовательных ошибок для каждого фильтра (`error_count` в таблице `sniper_filters`)
- Создана миграция Alembic для добавления поля
- Добавлены методы `increment_filter_error_count()` и `reset_filter_error_count()` в database adapter
- Добавлен метод `send_monitoring_error_notification()` в telegram_notifier
- При 3 последовательных ошибках пользователь получает уведомление

**Файлы:**
- `database.py` - добавлено поле `error_count`
- `alembic/versions/XXXXXXX_add_error_count_to_filters.py` - миграция
- `tender_sniper/database/sqlalchemy_adapter.py` - методы работы с error_count
- `tender_sniper/notifications/telegram_notifier.py` - уведомления об ошибках
- `tender_sniper/service.py` - логика отслеживания ошибок

### ✓ 2. Rate Limiting для zakupki.gov.ru
**Статус:** ✅ ИСПРАВЛЕНО

**Что сделано:**
- Добавлен rate limiter с минимальной задержкой 2 секунды между запросами
- Реализован метод `_wait_for_rate_limit()` с thread-safe блокировкой
- Rate limiting применяется ко всем запросам к zakupki.gov.ru (RSS и обогащение)

**Файлы:**
- `src/parsers/zakupki_rss_parser.py` - добавлен rate limiting

**Защита от:**
- Бан от zakupki.gov.ru при большом количестве пользователей
- Перегрузка их серверов

### ✓ 3. Увеличение таймаутов и retry логика
**Статус:** ✅ ИСПРАВЛЕНО

**Что сделано:**
- Увеличен таймаут для обогащения тендеров с 15 до 30 секунд
- Retry логика уже была реализована ранее (Retry strategy на уровне HTTP adapter)

**Файлы:**
- `src/parsers/zakupki_rss_parser.py` - таймаут 30с в `enrich_tender_from_page()`

---

## 🔶 ВАЖНЫЕ УЛУЧШЕНИЯ (Требуют реализации)

### 4. Миграция лимитов тарифных планов на PostgreSQL
**Приоритет:** ВЫСОКИЙ
**Время:** 2-3 часа
**Статус:** ❌ НЕ РЕАЛИЗОВАНО

**Проблема:**
Сейчас лимиты хардкодены в коде:
```python
max_filters = 5 if user['subscription_tier'] == 'free' else 15
daily_limit = 10 if subscription_tier == 'free' else 50
```

**Решение:**
1. Создать таблицу `subscription_plans` в PostgreSQL (структура уже есть в `tender_sniper/database/models.py`)
2. Перенести логику лимитов из кода в базу данных
3. Добавить админ-панель для управления тарифами

**Файлы для изменения:**
- `tender_sniper/database/models.py` - уже есть структура таблицы
- `tender_sniper/database/sqlalchemy_adapter.py` - добавить методы для работы с планами
- `tender_sniper/service.py` - использовать лимиты из БД
- `bot/handlers/admin_sniper.py` - админ-панель для тарифов

**Выгода:**
- Гибкое управление тарифами без деплоя
- A/B тестирование тарифов
- Персональные тарифы для VIP клиентов

---

### 5. Graceful Shutdown
**Приоритет:** ВЫСОКИЙ
**Время:** 1-2 часа
**Статус:** ❌ НЕ РЕАЛИЗОВАНО

**Проблема:**
При рестарте Railway может прервать отправку уведомлений или обработку тендеров. Это приведет к:
- Потере уведомлений
- Незаконченным транзакциям в БД
- Плохому UX

**Решение:**
```python
import signal
import asyncio

class GracefulShutdown:
    def __init__(self):
        self.shutdown_requested = False

    async def shutdown(self, signal, loop):
        logger.info(f"Получен сигнал {signal}, graceful shutdown...")
        self.shutdown_requested = True

        # Ждем завершения текущих задач (макс 30 секунд)
        tasks = [t for t in asyncio.all_tasks() if t is not asyncio.current_task()]
        await asyncio.wait(tasks, timeout=30)

        loop.stop()

# В main():
shutdown_handler = GracefulShutdown()
loop = asyncio.get_event_loop()
for sig in (signal.SIGTERM, signal.SIGINT):
    loop.add_signal_handler(sig, lambda s=sig: asyncio.create_task(shutdown_handler.shutdown(s, loop)))
```

**Файлы для изменения:**
- `bot/main.py` - добавить graceful shutdown handler

**Выгода:**
- Нет потери данных при рестартах
- Production-ready
- Railway может безопасно рестартить бота

---

### 6. Включить Health Check Endpoint
**Приоритет:** ВЫСОКИЙ
**Время:** 30 минут
**Статус:** ❌ ОТКЛЮЧЕН

**Проблема:**
```python
# bot/main.py:103
health_check_runner = None
logger.info("ℹ️  Health check server временно отключен")
```

Railway не может проверить, что бот работает корректно.

**Решение:**
1. Раскомментировать код health check в `bot/main.py`
2. Проверить, что health check endpoint работает (`:8080/health`)
3. Добавить проверку компонентов:
   - Database connectivity
   - Telegram API connectivity
   - RSS parser availability

**Файлы для изменения:**
- `bot/main.py` - раскомментировать health check
- `bot/health_check.py` - добавить проверки компонентов

**Выгода:**
- Railway может автоматически рестартить сломанные инстансы
- Мониторинг доступности
- Uptime SLA метрики

---

### 7. Валидация пользовательского ввода
**Приоритет:** СРЕДНИЙ
**Время:** 3-4 часа
**Статус:** ❌ НЕ РЕАЛИЗОВАНО

**Проблема:**
Нет валидации данных от пользователя. Риски:
- SQL injection (хотя используется ORM)
- XSS в HTML отчетах
- Некорректные данные в БД

**Примеры уязвимостей:**
```python
# Название фильтра
user_input = "<script>alert('xss')</script>"

# Ключевые слова
user_input = "'; DROP TABLE users; --"
```

**Решение:**
Использовать `pydantic` для валидации:

```python
from pydantic import BaseModel, Field, validator

class FilterCreate(BaseModel):
    name: str = Field(..., min_length=3, max_length=100)
    keywords: List[str] = Field(..., min_items=1, max_items=20)
    price_min: Optional[int] = Field(None, ge=0, le=1_000_000_000)
    price_max: Optional[int] = Field(None, ge=0, le=1_000_000_000)

    @validator('name')
    def validate_name(cls, v):
        # Запрещаем HTML теги
        if '<' in v or '>' in v:
            raise ValueError('HTML теги запрещены')
        return v.strip()

    @validator('keywords')
    def validate_keywords(cls, v):
        return [kw.strip() for kw in v if kw.strip()]
```

**Файлы для изменения:**
- Создать `bot/schemas/filters.py` - Pydantic модели
- `bot/handlers/sniper_search.py` - использовать схемы для валидации
- `tender_sniper/all_tenders_report.py` - экранировать HTML

**Выгода:**
- Безопасность
- Чистые данные в БД
- Понятные ошибки для пользователей

---

### 8. Логирование действий пользователей
**Приоритет:** СРЕДНИЙ
**Время:** 2-3 часа
**Статус:** ❌ НЕ РЕАЛИЗОВАНО

**Проблема:**
Невозможно понять:
- Какие фильтры используются чаще всего
- Где пользователи застревают
- Какие функции популярны

**Решение:**
Создать таблицу `user_actions`:

```sql
CREATE TABLE user_actions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES sniper_users(id),
    action_type VARCHAR(50) NOT NULL,  -- 'filter_created', 'search_executed', 'tender_viewed'
    action_data JSONB,  -- Дополнительные данные
    created_at TIMESTAMP DEFAULT NOW()
);
```

**События для логирования:**
- `filter_created` - создан фильтр
- `filter_edited` - изменен фильтр
- `filter_deleted` - удален фильтр
- `search_executed` - выполнен мгновенный поиск
- `tender_viewed` - просмотрен тендер
- `tender_favorited` - добавлен в избранное
- `tender_hidden` - скрыт тендер

**Файлы для создания:**
- `alembic/versions/XXX_create_user_actions.py` - миграция
- `database.py` - модель UserAction
- `bot/utils/analytics.py` - helper для логирования

**Выгода:**
- Понимание поведения пользователей
- Данные для улучшения продукта
- Обнаружение проблем UX

---

## 🔵 ЖЕЛАТЕЛЬНЫЕ УЛУЧШЕНИЯ (Nice to have)

### 9. Unit и Integration тесты
**Приоритет:** СРЕДНИЙ
**Время:** 1-2 дня
**Статус:** ❌ ЧАСТИЧНО (только test_smart_matcher.py)

**Критичные области для тестирования:**
1. **RSS Парсинг:**
   ```python
   def test_rss_parser_filters_services():
       parser = ZakupkiRSSParser()
       tenders = parser.search_tenders_rss(
           keywords="медицинские",
           tender_type="товары"
       )
       # Проверяем, что нет услуг
       for tender in tenders:
           assert "оказание услуг" not in tender['name'].lower()
   ```

2. **Smart Matching:**
   - Уже есть: `tests/unit/test_smart_matcher.py`
   - Добавить тесты на edge cases

3. **Database Operations:**
   ```python
   @pytest.mark.asyncio
   async def test_filter_error_count():
       db = TenderSniperDB()
       filter_id = await db.create_filter(...)

       # Увеличиваем счетчик
       count1 = await db.increment_filter_error_count(filter_id)
       assert count1 == 1

       # Сбрасываем
       await db.reset_filter_error_count(filter_id)
       # ...
   ```

**Файлы для создания:**
- `tests/unit/test_rss_parser.py`
- `tests/unit/test_database.py`
- `tests/integration/test_tender_flow.py`

**Выгода:**
- Уверенность при рефакторинге
- Быстрое обнаружение регрессий
- Документация через примеры

---

### 10. Уведомления о квоте
**Приоритет:** НИЗКИЙ
**Время:** 1 час
**Статус:** ✅ ЧАСТИЧНО РЕАЛИЗОВАНО

**Что есть:**
- `send_quota_exceeded_notification()` - уже реализовано

**Что добавить:**
- Проактивное уведомление при 80% квоты:
```python
if daily_count >= daily_limit * 0.8:
    await notifier.send_quota_warning(telegram_id, daily_count, daily_limit)
```

**Файлы для изменения:**
- `tender_sniper/service.py` - добавить проверку 80% квоты
- `tender_sniper/notifications/telegram_notifier.py` - метод send_quota_warning

---

### 11. Экспорт истории в CSV/Excel
**Приоритет:** НИЗКИЙ
**Время:** 2-3 часа
**Статус:** ❌ НЕ РЕАЛИЗОВАНО

**Функционал:**
Кнопка "📥 Экспорт" в "Все мои тендеры" → файл CSV/Excel с:
- Номер тендера
- Название
- Цена
- Регион
- Срок подачи
- URL
- Дата находки

**Реализация:**
```python
import pandas as pd
from io import BytesIO

async def export_tenders_to_excel(tenders: List[Dict]) -> BytesIO:
    df = pd.DataFrame(tenders)
    buffer = BytesIO()
    df.to_excel(buffer, index=False, engine='openpyxl')
    buffer.seek(0)
    return buffer

# В обработчике:
buffer = await export_tenders_to_excel(tenders)
await message.answer_document(
    BufferedInputFile(buffer.read(), filename='tenders.xlsx'),
    caption="📊 Экспорт ваших тендеров"
)
```

**Файлы для создания:**
- `bot/utils/export.py` - функции экспорта
- `bot/handlers/all_tenders.py` - кнопка экспорта

**Зависимости:**
```
pandas>=2.0.0
openpyxl>=3.1.0
```

---

### 12. Подробная /help документация
**Приоритет:** НИЗКИЙ
**Время:** 1-2 часа
**Статус:** ❌ БАЗОВАЯ

**Что добавить:**
- FAQ с примерами
- Гайд по созданию эффективных фильтров
- Объяснение scoring системы
- Troubleshooting

**Структура:**
```
/help
  ├─ 📖 Быстрый старт
  ├─ 🎯 Создание фильтров
  ├─ 📊 Понимание результатов
  ├─ ⚙️ Настройки
  ├─ ❓ FAQ
  └─ 📞 Поддержка
```

**Файлы для создания:**
- `bot/handlers/help.py` - подробная помощь
- `bot/utils/help_messages.py` - тексты справки

---

### 13. Кеширование RSS результатов
**Приоритет:** НИЗКИЙ
**Время:** 2-3 часа
**Статус:** ❌ НЕ РЕАЛИЗОВАНО

**Проблема:**
Повторные поиски по одинаковым параметрам делают лишние запросы к zakupki.gov.ru

**Решение:**
```python
import hashlib
from datetime import datetime, timedelta

class RSSCache:
    def __init__(self, ttl_minutes=5):
        self.cache = {}
        self.ttl = timedelta(minutes=ttl_minutes)

    def get_cache_key(self, **params):
        key_str = json.dumps(params, sort_keys=True)
        return hashlib.md5(key_str.encode()).hexdigest()

    def get(self, **params):
        key = self.get_cache_key(**params)
        if key in self.cache:
            cached_at, data = self.cache[key]
            if datetime.now() - cached_at < self.ttl:
                return data
        return None

    def set(self, data, **params):
        key = self.get_cache_key(**params)
        self.cache[key] = (datetime.now(), data)
```

**Файлы для создания:**
- `src/parsers/rss_cache.py` - кеш
- `src/parsers/zakupki_rss_parser.py` - использование кеша

**Выгода:**
- Снижение нагрузки на zakupki.gov.ru
- Быстрее ответы пользователям
- Экономия прокси трафика

---

### 14. Мониторинг использования API (OpenAI/Anthropic)
**Приоритет:** НИЗКИЙ
**Время:** 1 час
**Статус:** ❌ НЕ РЕАЛИЗОВАНО

**Проблема:**
Не видим сколько тратим на AI запросы

**Решение:**
```python
class AIUsageTracker:
    def __init__(self):
        self.usage = {
            'total_tokens': 0,
            'total_cost_usd': 0,
            'requests_count': 0
        }

    def track_request(self, provider, model, tokens, cost):
        self.usage['total_tokens'] += tokens
        self.usage['total_cost_usd'] += cost
        self.usage['requests_count'] += 1

        logger.info(f"AI Usage: {model} - {tokens} tokens - ${cost:.4f}")
```

**Файлы для создания:**
- `bot/utils/ai_usage_tracker.py` - трекер
- `tender_sniper/ai_name_generator.py` - логирование использования

---

### 15. Telegram Mini App (Web App)
**Приоритет:** ОЧЕНЬ НИЗКИЙ
**Время:** 2-3 недели
**Статус:** ❌ НЕ РЕАЛИЗОВАНО

**Зачем:**
- Более богатый UI
- Таблицы с сортировкой
- Графики статистики
- Drag & drop фильтров

**Технологии:**
- React/Vue.js
- Telegram Web App API
- REST API для бота

**Выгода:**
- Premium UX
- Конкурентное преимущество
- Удобнее для power users

---

## 📊 ПРИОРИТИЗАЦИЯ ЗАДАЧ

### MVP для первых 10-20 пользователей (1-2 дня):
1. ✅ Уведомления о проблемах мониторинга (СДЕЛАНО)
2. ✅ Rate limiting (СДЕЛАНО)
3. ✅ Увеличение таймаутов (СДЕЛАНО)
4. ⚠️ Включить health check (30 мин)
5. ⚠️ Базовая валидация ввода (2 часа)

### Для масштабирования до 100+ пользователей (1 неделя):
6. Миграция лимитов на PostgreSQL (2-3 часа)
7. Graceful shutdown (1-2 часа)
8. Подробная /help документация (1-2 часа)
9. Логирование действий пользователей (2-3 часа)

### Production-ready (2-3 недели):
10. Unit/Integration тесты (1-2 дня)
11. Кеширование RSS (2-3 часа)
12. Мониторинг AI usage (1 час)
13. Экспорт в CSV/Excel (2-3 часа)

---

## 🎯 РЕКОМЕНДУЕМЫЙ ПЛАН ДЕЙСТВИЙ

### Неделя 1 (Критичное):
- [x] Исправить уведомления об ошибках ✅
- [x] Добавить rate limiting ✅
- [x] Увеличить таймауты ✅
- [ ] Включить health check
- [ ] Добавить базовую валидацию

### Неделя 2 (Важное):
- [ ] Миграция тарифов на PostgreSQL
- [ ] Graceful shutdown
- [ ] Логирование пользовательских действий
- [ ] Подробная документация /help

### Неделя 3-4 (Желательное):
- [ ] Unit тесты для критичных компонентов
- [ ] Кеширование RSS
- [ ] Экспорт в Excel
- [ ] Мониторинг AI usage

---

## 📝 ЗАМЕТКИ

**Готовность к продакшену:** ~75%

**Блокирующие проблемы:** НЕТ
**Критичные проблемы:** ИСПРАВЛЕНЫ
**Можно запускать пользователей:** ДА (с оговорками)

**Оговорки:**
- Health check отключен (не критично, но желательно включить)
- Нет валидации ввода (средний риск)
- Нет graceful shutdown (может быть неприятно при рестартах)

**Рекомендация:**
Можно запускать с первыми 10-20 пользователями СЕЙЧАС, но обязательно сделать health check и валидацию в течение недели.

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

- [Railway Docs](https://docs.railway.app/)
- [Aiogram 3.x Docs](https://docs.aiogram.dev/en/latest/)
- [Pydantic Validation](https://docs.pydantic.dev/)
- [PostgreSQL Best Practices](https://wiki.postgresql.org/wiki/Don%27t_Do_This)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Дата создания:** 10 декабря 2024
**Автор:** Claude (Sonnet 4.5)
**Версия:** 1.0
